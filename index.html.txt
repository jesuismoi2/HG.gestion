<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="apple-mobile-web-app-title" content="HC Gestion">
<meta name="mobile-web-app-capable" content="yes">
<meta name="theme-color" content="#1F3864">
<meta name="description" content="Gestion comptable Hexagone Confluence">
<link rel="manifest" href="manifest.json">
<!-- Icône PWA inline via canvas au chargement -->
<title>HC Gestion — Hexagone Confluence</title>
<style>
:root {
  --bleu:#1F3864;--bleu-l:#D6E4F0;--vert:#1E5631;--vert-l:#D9EAD3;
  --orng:#7F3F00;--orng-l:#FCE5CD;--gris:#2C3E50;--gris-l:#F4F6F9;
  --jaune:#FFF2CC;--rouge:#E74C3C;--vert-ok:#27AE60;--blanc:#FFFFFF;
  --texte:#1a1a2e;--border:#DDE2EC;--shadow:0 2px 12px rgba(31,56,100,.10);
  --radius:10px;--caisse:#4A235A;--tva:#1B4F72;
}
*{box-sizing:border-box;margin:0;padding:0;}
body{font-family:-apple-system,BlinkMacSystemFont,'Helvetica Neue',sans-serif;
  background:var(--gris-l);color:var(--texte);min-height:100vh;overflow-x:hidden;}

/* HEADER */
.app-header{background:var(--bleu);color:white;padding:14px 16px 10px;
  position:sticky;top:0;z-index:100;box-shadow:0 2px 8px rgba(0,0,0,.2);}
.header-top{display:flex;align-items:center;justify-content:space-between;}
.app-title{font-size:19px;font-weight:700;letter-spacing:-.3px;}
.app-sub{font-size:11px;opacity:.7;margin-top:1px;}
.header-btns{display:flex;gap:8px;}
.hbtn{background:rgba(255,255,255,.15);border:1px solid rgba(255,255,255,.3);
  color:white;padding:7px 12px;border-radius:18px;font-size:12px;cursor:pointer;
  display:flex;align-items:center;gap:5px;}
.hbtn:active{background:rgba(255,255,255,.25);}

/* NAV */
.nav-tabs{background:var(--bleu);display:flex;overflow-x:auto;scrollbar-width:none;
  border-top:1px solid rgba(255,255,255,.1);}
.nav-tabs::-webkit-scrollbar{display:none;}
.nav-tab{flex:none;padding:9px 14px;color:rgba(255,255,255,.6);font-size:12px;
  font-weight:500;cursor:pointer;white-space:nowrap;border-bottom:3px solid transparent;}
.nav-tab.active{color:white;border-bottom-color:#64B5F6;}

/* PAGES */
.page{display:none;padding:14px;padding-bottom:80px;}
.page.active{display:block;}

/* CARDS */
.card{background:white;border-radius:var(--radius);box-shadow:var(--shadow);
  margin-bottom:14px;overflow:hidden;}
.ch{padding:12px 14px;font-weight:600;font-size:13px;display:flex;
  align-items:center;justify-content:space-between;border-bottom:1px solid var(--border);}
.ch.bleu{background:var(--bleu);color:white;}
.ch.vert{background:var(--vert);color:white;}
.ch.orng{background:var(--orng);color:white;}
.ch.gris{background:var(--gris);color:white;}
.ch.tva{background:var(--tva);color:white;}
.cb{padding:14px;}

/* BOUTONS */
.btn{padding:9px 16px;border-radius:8px;font-size:13px;font-weight:600;border:none;
  cursor:pointer;display:inline-flex;align-items:center;gap:5px;transition:opacity .15s;}
.btn:active{opacity:.8;}
.btn-p{background:var(--bleu);color:white;}
.btn-v{background:var(--vert);color:white;}
.btn-o{background:var(--orng);color:white;}
.btn-r{background:var(--rouge);color:white;}
.btn-g{background:var(--gris-l);color:var(--texte);border:1px solid var(--border);}
.btn-sm{padding:5px 10px;font-size:11px;}
.btn-w{width:100%;justify-content:center;}
.btn-wh{background:rgba(255,255,255,.2);color:white;border:none;padding:5px 10px;
  border-radius:6px;font-size:11px;cursor:pointer;font-weight:600;}

/* FORMES */
.fg{margin-bottom:12px;}
.fg label{display:block;font-size:11px;font-weight:700;color:#666;margin-bottom:3px;
  text-transform:uppercase;letter-spacing:.3px;}
.fg input,.fg select,.fg textarea{width:100%;padding:9px 11px;border:1px solid var(--border);
  border-radius:8px;font-size:14px;font-family:inherit;background:white;color:var(--texte);
  -webkit-appearance:none;}
.fg input:focus,.fg select:focus,.fg textarea:focus{outline:none;border-color:var(--bleu);
  box-shadow:0 0 0 3px rgba(31,56,100,.1);}
.fg.j input,.fg.j select,.fg.j textarea{background:var(--jaune);}
.fr{display:grid;grid-template-columns:1fr 1fr;gap:10px;}
.fr3{display:grid;grid-template-columns:1fr 1fr 1fr;gap:10px;}
.fr4{display:grid;grid-template-columns:1fr 1fr 1fr 1fr;gap:8px;}

/* TABLEAUX */
.tw{overflow-x:auto;-webkit-overflow-scrolling:touch;}
table{width:100%;border-collapse:collapse;font-size:12px;}
th{background:var(--bleu);color:white;padding:8px 6px;text-align:left;
  font-size:10px;font-weight:700;white-space:nowrap;}
td{padding:8px 6px;border-bottom:1px solid var(--border);vertical-align:middle;}
tr:last-child td{border-bottom:none;}
tr.row-v td{background:rgba(39,174,96,.07);}
tr.row-r td{background:rgba(231,76,60,.07);}
tr:hover td{background:rgba(31,56,100,.03);}

/* KPI */
.kpi-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:14px;}
.kpi{background:white;border-radius:var(--radius);padding:12px;box-shadow:var(--shadow);
  border-left:4px solid var(--bleu);}
.kpi.v{border-left-color:var(--vert);}
.kpi.o{border-left-color:var(--orng);}
.kpi.r{border-left-color:var(--rouge);}
.kpi.c{border-left-color:var(--caisse);}
.kpi.t{border-left-color:var(--tva);}
.kl{font-size:10px;color:#888;font-weight:700;text-transform:uppercase;letter-spacing:.3px;}
.kv{font-size:20px;font-weight:800;color:var(--texte);margin-top:3px;}
.ks{font-size:10px;color:#aaa;margin-top:1px;}

/* BADGES */
.badge{display:inline-block;padding:2px 7px;border-radius:10px;font-size:10px;font-weight:700;text-transform:uppercase;}
.bv{background:var(--vert-l);color:var(--vert);}
.br{background:#FADBD8;color:var(--rouge);}
.bo{background:var(--orng-l);color:var(--orng);}
.bb{background:var(--bleu-l);color:var(--bleu);}
.bg{background:var(--gris-l);color:var(--gris);}

/* ACCORDION */
.accord-item{border-bottom:1px solid var(--border);}
.accord-item:last-child{border-bottom:none;}
.accord-header{display:flex;align-items:center;justify-content:space-between;
  padding:12px 14px;cursor:pointer;user-select:none;}
.accord-header:active{background:var(--gris-l);}
.accord-arrow{transition:transform .2s;font-size:16px;}
.accord-body{display:none;padding:0 14px 14px;background:var(--gris-l);}
.accord-item.open .accord-arrow{transform:rotate(180deg);}
.accord-item.open .accord-body{display:block;}

/* MODAL */
.modal-overlay{display:none;position:fixed;inset:0;background:rgba(0,0,0,.5);
  z-index:200;align-items:flex-end;}
.modal-overlay.open{display:flex;}
.modal{background:white;border-radius:16px 16px 0 0;width:100%;max-height:93vh;
  overflow-y:auto;padding:18px;padding-bottom:34px;}
.modal-handle{width:38px;height:4px;background:#ddd;border-radius:2px;margin:0 auto 14px;}
.modal-title{font-size:17px;font-weight:700;margin-bottom:14px;}

/* EMPTY */
.empty{text-align:center;padding:36px 20px;color:#bbb;}
.ei{font-size:36px;margin-bottom:10px;}
.et{font-size:13px;}

/* TOAST */
.toast{position:fixed;bottom:20px;left:50%;transform:translateX(-50%) translateY(100px);
  background:var(--gris);color:white;padding:10px 18px;border-radius:22px;font-size:13px;
  font-weight:500;z-index:300;transition:transform .3s;white-space:nowrap;}
.toast.show{transform:translateX(-50%) translateY(0);}

/* MONTANTS */
.mp{color:var(--vert);font-weight:700;}
.mn{color:var(--rouge);font-weight:700;}
.mt{font-variant-numeric:tabular-nums;}

/* CODE MISSION */
.cm{font-family:'SF Mono',monospace;font-size:11px;font-weight:700;color:var(--vert);
  background:var(--vert-l);padding:2px 5px;border-radius:4px;}

/* SECTION TITLE */
.st{font-size:11px;font-weight:700;color:#999;text-transform:uppercase;
  letter-spacing:.4px;margin:16px 0 8px;}

/* TABS internes */
.inner-tabs{display:flex;border-bottom:1px solid var(--border);margin-bottom:14px;}
.inner-tab{padding:8px 14px;font-size:12px;font-weight:600;color:#888;cursor:pointer;
  border-bottom:2px solid transparent;}
.inner-tab.active{color:var(--bleu);border-bottom-color:var(--bleu);}

/* BON */
.bon-page{background:white;border:1px solid var(--border);border-radius:8px;
  overflow:hidden;margin-top:12px;}
.bon-head{padding:14px;text-align:center;}
.bon-body{padding:14px;}
.bon-section{margin-bottom:14px;padding-bottom:14px;border-bottom:1px solid var(--border);}
.bon-section:last-child{border-bottom:none;margin-bottom:0;}
.bon-label{font-size:10px;font-weight:700;color:#888;text-transform:uppercase;margin-bottom:3px;}
.bon-val{font-size:14px;font-weight:600;}

/* BILAN */
.bilan-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px;}
.bilan-col{border-radius:8px;overflow:hidden;}
.bilan-head{padding:10px 12px;font-weight:700;font-size:13px;color:white;text-align:center;}
.bilan-row{display:flex;justify-content:space-between;align-items:center;
  padding:10px 12px;border-bottom:1px solid var(--border);font-size:13px;}
.bilan-row:last-child{border-bottom:none;}
.bilan-total{display:flex;justify-content:space-between;padding:10px 12px;
  font-weight:700;font-size:14px;}

/* PRINT */
@media print {
  .app-header,.nav-tabs,.nav-btns,.btn,.hbtn,#toast{display:none!important;}
  .page{display:block!important;padding:0;}
  .modal-overlay{display:none!important;}
  .no-print{display:none!important;}
  body{background:white;}
  .bon-page{border:none;box-shadow:none;}
}

@media(min-width:600px){
  .kpi-grid{grid-template-columns:repeat(4,1fr);}
  .page{padding:18px;padding-bottom:80px;max-width:1000px;margin:0 auto;}
}
</style>
</head>
<body>

<div class="app-header">
  <div class="header-top">
    <div>
      <div class="app-title">🚗 HC Gestion</div>
      <div class="app-sub">Hexagone Confluence • <span id="mois-label"></span></div>
    </div>
    <div class="header-btns">
      <button class="hbtn" onclick="ouvrirModalMois()">📅 Mois</button>
      <button class="hbtn" onclick="syncDrive()">☁️ Drive</button>
    </div>
  </div>
</div>

<div class="nav-tabs" id="nav-tabs">
  <div class="nav-tab active" onclick="showPage('dashboard',this)">📊 Accueil</div>
  <div class="nav-tab" onclick="showPage('missions',this)">📋 Missions</div>
  <div class="nav-tab" onclick="showPage('compta',this)">🧾 Comptabilité</div>
  <div class="nav-tab" onclick="showPage('convoyeurs',this)">🚗 Convoyeurs</div>
  <div class="nav-tab" onclick="showPage('clients',this)">🏢 Clients</div>
  <div class="nav-tab" onclick="showPage('bons',this)">📄 Bons</div>
  <div class="nav-tab" onclick="showPage('bilan',this)">📊 Bilan</div>
  <div class="nav-tab" onclick="showPage('refs',this)">⚙️ Réf.</div>
</div>

<!-- ═══════════════════════════════ DASHBOARD ═══════════════════════════════ -->
<div id="page-dashboard" class="page active">
  <div class="kpi-grid">
    <div class="kpi v"><div class="kl">CA HT</div><div class="kv" id="k-ca">0 €</div><div class="ks" id="k-nb-miss">0 mission(s)</div></div>
    <div class="kpi"><div class="kl">Solde banque</div><div class="kv" id="k-banque">0 €</div><div class="ks">Compte pro</div></div>
    <div class="kpi o"><div class="kl">Dû convoyeurs</div><div class="kv" id="k-conv">0 €</div><div class="ks" id="k-conv-nb">0 non payé(s)</div></div>
    <div class="kpi t"><div class="kl">TVA à reverser</div><div class="kv" id="k-tva">0 €</div><div class="ks">Solde cumulé</div></div>
    <div class="kpi r"><div class="kl">À encaisser</div><div class="kv" id="k-enc">0 €</div><div class="ks" id="k-enc-nb">0 client(s)</div></div>
    <div class="kpi c"><div class="kl">Caisse espèces</div><div class="kv" id="k-caisse">0 €</div><div class="ks">Espèces en main</div></div>
    <div class="kpi"><div class="kl">Bénéfice HT</div><div class="kv mp" id="k-benef">0 €</div><div class="ks">Ce mois</div></div>
    <div class="kpi"><div class="kl">Dette CCA</div><div class="kv" id="k-cca">0 €</div><div class="ks">Société te doit</div></div>
  </div>

  <div class="card">
    <div class="ch vert">📋 Missions récentes <button class="btn-wh" onclick="showPage('missions',document.querySelectorAll('.nav-tab')[1])">Tout voir</button></div>
    <div id="dash-missions"><div class="empty"><div class="ei">📋</div><div class="et">Aucune mission</div></div></div>
  </div>

  <div class="card">
    <div class="ch orng">🚗 Convoyeurs à payer <button class="btn-wh" onclick="showPage('convoyeurs',document.querySelectorAll('.nav-tab')[3])">Tout voir</button></div>
    <div id="dash-conv"><div class="empty"><div class="ei">✅</div><div class="et">Tous à jour</div></div></div>
  </div>

  <div class="card">
    <div class="ch bleu">🏢 Clients en attente <button class="btn-wh" onclick="showPage('clients',document.querySelectorAll('.nav-tab')[4])">Tout voir</button></div>
    <div id="dash-cli"><div class="empty"><div class="ei">✅</div><div class="et">Tous ont payé</div></div></div>
  </div>
</div>

<!-- ═══════════════════════════════ MISSIONS ═══════════════════════════════ -->
<div id="page-missions" class="page">
  <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:12px;">
    <div><div style="font-size:17px;font-weight:700;">Dépenses Mission</div><div class="ks" id="miss-count"></div></div>
    <button class="btn btn-v btn-sm" onclick="openModal('modal-mission')">+ Nouvelle</button>
  </div>

  <!-- Totaux missions -->
  <div class="card">
    <div class="ch vert">Totaux du mois</div>
    <div class="cb">
      <div class="fr4">
        <div><div class="kl">CA HT</div><div class="mp mt" id="tot-ca" style="font-size:16px;font-weight:800;">0 €</div></div>
        <div><div class="kl">Dépenses HT</div><div class="mn mt" id="tot-dep" style="font-size:16px;font-weight:800;">0 €</div></div>
        <div><div class="kl">Convoyeurs HT</div><div class="mn mt" id="tot-conv" style="font-size:16px;font-weight:800;">0 €</div></div>
        <div><div class="kl">Bénéfice HT</div><div class="mp mt" id="tot-ben" style="font-size:16px;font-weight:800;">0 €</div></div>
      </div>
      <div class="fr" style="margin-top:10px;">
        <div><div class="kl">TVA collectée</div><div class="mt" id="tot-tva-c" style="font-size:14px;font-weight:700;color:var(--tva);">0 €</div></div>
        <div><div class="kl">TVA déductible</div><div class="mt" id="tot-tva-d" style="font-size:14px;font-weight:700;color:var(--vert);">0 €</div></div>
      </div>
    </div>
  </div>

  <div class="card">
    <div class="tw">
      <table id="table-miss">
        <thead><tr>
          <th>Date</th><th>Code mission</th><th>N° Facture</th><th>Client</th><th>Convoyeur</th>
          <th>Prix HT</th><th>Prix TTC</th><th>Carbu.</th><th>Péage</th><th>Lavage</th>
          <th>Repas FF</th><th>Repas SM</th><th>Transp.A</th><th>Transp.R</th><th>HT s/TVA</th><th>Autre</th>
          <th>Total dép.TTC</th><th>Total dép.HT</th><th>Conv.HT</th><th>Bénéfice HT</th>
          <th>TVA coll.</th><th>TVA déd.</th><th>Statut</th><th>Lien</th><th></th>
        </tr></thead>
        <tbody id="tbody-miss"></tbody>
      </table>
    </div>
  </div>
</div>

<!-- ═══════════════════════════════ COMPTABILITÉ ═══════════════════════════════ -->
<div id="page-compta" class="page">
  <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:12px;">
    <div><div style="font-size:17px;font-weight:700;">Comptabilité</div><div class="ks">Comptabilité d'encaissement</div></div>
    <div style="display:flex;gap:8px;">
      <button class="btn btn-g btn-sm" onclick="openModal('modal-recurrentes')">🔄 Récurrences</button>
      <button class="btn btn-p btn-sm" onclick="openModal('modal-compta')">+ Écriture</button>
    </div>
  </div>

  <div class="kpi-grid">
    <div class="kpi"><div class="kl">Solde banque</div><div class="kv" id="c-banque">0 €</div></div>
    <div class="kpi c"><div class="kl">Caisse espèces</div><div class="kv" id="c-caisse">0 €</div></div>
    <div class="kpi"><div class="kl">Dette CCA</div><div class="kv" id="c-cca">0 €</div></div>
    <div class="kpi t"><div class="kl">Solde TVA dû</div><div class="kv mn" id="c-tva">0 €</div></div>
  </div>

  <!-- Récap TVA -->
  <div class="card">
    <div class="ch tva">Récapitulatif TVA</div>
    <div class="cb">
      <div class="fr3">
        <div><div class="kl">TVA collectée</div><div class="mt" style="font-size:16px;font-weight:800;color:var(--tva);" id="r-tva-c">0 €</div></div>
        <div><div class="kl">TVA déductible</div><div class="mt" style="font-size:16px;font-weight:800;color:var(--vert);" id="r-tva-d">0 €</div></div>
        <div><div class="kl">Solde TVA dû</div><div class="mt mn" style="font-size:16px;font-weight:800;" id="r-tva-n">0 €</div></div>
      </div>
    </div>
  </div>

  <div class="card">
    <div class="tw">
      <table id="table-compta">
        <thead><tr>
          <th>Date</th><th>Type</th><th>Catégorie</th><th>Sous-cat.</th>
          <th>Convoyeur</th><th>Client</th><th>Réf./Facture</th><th>Descriptif</th>
          <th>Non déd.</th><th>5,5%</th><th>10%</th><th>20%</th><th>Sans TVA</th>
          <th>Total TTC</th><th>Total HT</th>
          <th>TVA 5,5%</th><th>TVA 10%</th><th>TVA 20%</th>
          <th>TVA coll.</th><th>TVA déd.</th>
          <th>Solde banque</th><th>Solde caisse</th><th>Dette CCA</th><th>Solde TVA</th>
          <th>Justif.</th><th></th>
        </tr></thead>
        <tbody id="tbody-compta"></tbody>
      </table>
    </div>
  </div>
</div>

<!-- ═══════════════════════════════ CONVOYEURS ═══════════════════════════════ -->
<div id="page-convoyeurs" class="page">
  <div style="font-size:17px;font-weight:700;margin-bottom:14px;">Paiement Convoyeurs</div>
  <div id="liste-conv"></div>
</div>

<!-- ═══════════════════════════════ CLIENTS ═══════════════════════════════ -->
<div id="page-clients" class="page">
  <div style="font-size:17px;font-weight:700;margin-bottom:14px;">Suivi Clients</div>
  <div id="liste-cli"></div>
</div>

<!-- ═══════════════════════════════ BONS ═══════════════════════════════ -->
<div id="page-bons" class="page">
  <div class="inner-tabs">
    <div class="inner-tab active" onclick="switchBon('client',this)">📋 Bon Client</div>
    <div class="inner-tab" onclick="switchBon('convoyeur',this)">🚗 Bon Convoyeur</div>
  </div>

  <div id="bon-client-panel">
    <div class="fg j"><label>Mission</label><select id="bon-miss-sel" onchange="chargerBon('client')"><option value="">-- Choisir --</option></select></div>
    <div id="bon-client-content" style="display:none;">
      <div class="bon-page" id="bon-client-print">
        <div class="bon-head" style="background:var(--bleu);color:white;">
          <div style="font-size:17px;font-weight:700;">BON DE COMMANDE CLIENT</div>
          <div style="font-size:11px;opacity:.8;">HEXAGONE CONFLUENCE — SIREN 102 530 596 000 12 — RCS Versailles</div>
        </div>
        <div class="bon-body">
          <div class="bon-section">
            <div class="fr3">
              <div><div class="bon-label">Code mission</div><div class="cm" id="bc-code" style="font-size:13px;"></div></div>
              <div><div class="bon-label">N° Facture</div><div class="bon-val" id="bc-fac"></div></div>
              <div><div class="bon-label">Date</div><div class="bon-val" id="bc-date"></div></div>
            </div>
            <div class="fr" style="margin-top:10px;">
              <div><div class="bon-label">Client</div><div class="bon-val" id="bc-client"></div></div>
              <div><div class="bon-label">Convoyeur</div><div class="bon-val" id="bc-conv"></div></div>
            </div>
            <div class="fr" style="margin-top:10px;">
              <div><div class="bon-label">Prix mission HT</div><div class="bon-val mp" id="bc-ht" style="font-size:16px;"></div></div>
              <div><div class="bon-label">Prix mission TTC</div><div class="bon-val mp" id="bc-ttc" style="font-size:16px;"></div></div>
            </div>
          </div>
          <div class="bon-section">
            <div style="font-weight:700;color:var(--vert);margin-bottom:8px;">📍 DÉPART</div>
            <div class="fg j"><label>Adresse de départ</label><input type="text" id="bc-dep-adr" placeholder="Adresse complète…"></div>
            <div class="fr">
              <div class="fg j"><label>Contact / N° client départ</label><input type="text" id="bc-dep-tel" placeholder="Nom, téléphone…"></div>
              <div class="fg j"><label>Heure prise en charge</label><input type="time" id="bc-dep-h"></div>
            </div>
          </div>
          <div class="bon-section">
            <div style="font-weight:700;color:var(--orng);margin-bottom:8px;">🏁 ARRIVÉE</div>
            <div class="fg j"><label>Adresse d'arrivée</label><input type="text" id="bc-arr-adr" placeholder="Adresse complète…"></div>
            <div class="fr">
              <div class="fg j"><label>Contact / N° client arrivée</label><input type="text" id="bc-arr-tel" placeholder="Nom, téléphone…"></div>
              <div class="fg j"><label>Heure de livraison</label><input type="time" id="bc-arr-h"></div>
            </div>
          </div>
          <div class="bon-section">
            <div style="font-weight:700;margin-bottom:8px;">🚗 VÉHICULE</div>
            <div class="fr3">
              <div class="fg j"><label>Marque / Modèle</label><input type="text" id="bc-modele" placeholder="Ex: Peugeot 308"></div>
              <div class="fg j"><label>Immatriculation</label><input type="text" id="bc-immat" placeholder="AB-123-CD"></div>
              <div class="fg j"><label>Couleur</label><input type="text" id="bc-couleur" placeholder="Blanc…"></div>
            </div>
          </div>
          <div class="bon-section">
            <div style="font-weight:700;margin-bottom:8px;">✅ OPTIONS</div>
            <div id="bc-options"></div>
            <button class="btn btn-g btn-sm" onclick="ajouterOption('client')" style="margin-top:6px;">+ Ajouter option</button>
            <div style="margin-top:8px;font-size:12px;font-weight:700;color:var(--bleu);">Total options HT : <span id="bc-total-options">0,00 €</span></div>
          </div>
          <div class="bon-section">
            <div style="font-weight:700;margin-bottom:8px;">📋 INSTRUCTIONS</div>
            <div class="fg j"><textarea id="bc-instruct" rows="3" placeholder="Instructions particulières…"></textarea></div>
          </div>
          <div style="background:var(--gris-l);padding:10px;border-radius:6px;font-size:11px;color:#888;text-align:center;">
            HEXAGONE CONFLUENCE — SIREN 102 530 596 000 12 — RCS Versailles — TVA FR05102536596
          </div>
        </div>
      </div>
      <button class="btn btn-p btn-w no-print" style="margin-top:12px;" onclick="imprimerBon()">🖨️ Générer PDF</button>
    </div>
  </div>

  <div id="bon-conv-panel" style="display:none;">
    <div class="fg j"><label>Mission</label><select id="bon-conv-sel" onchange="chargerBon('convoyeur')"><option value="">-- Choisir --</option></select></div>
    <div id="bon-conv-content" style="display:none;">
      <div class="bon-page" id="bon-conv-print">
        <div class="bon-head" style="background:var(--vert);color:white;">
          <div style="font-size:17px;font-weight:700;">BON DE MISSION CONVOYEUR</div>
          <div style="font-size:11px;opacity:.8;">HEXAGONE CONFLUENCE — SIREN 102 530 596 000 12</div>
        </div>
        <div class="bon-body">
          <div class="bon-section">
            <div class="fr3">
              <div><div class="bon-label">Code mission</div><div class="cm" id="bv-code" style="font-size:13px;"></div></div>
              <div><div class="bon-label">Date</div><div class="bon-val" id="bv-date"></div></div>
              <div><div class="bon-label">Convoyeur</div><div class="bon-val" id="bv-conv"></div></div>
            </div>
            <div class="fr" style="margin-top:10px;">
              <div><div class="bon-label">Rémunération HT</div><div class="bon-val mp" id="bv-remun" style="font-size:16px;"></div></div>
              <div><div class="bon-label">Mode de paiement</div><div class="fg j" style="margin:0;"><select id="bv-mode"><option>Virement</option><option>Espèces</option><option>Chèque</option></select></div></div>
            </div>
          </div>
          <div class="bon-section">
            <div style="font-weight:700;color:var(--vert);margin-bottom:8px;">📍 DÉPART</div>
            <div class="fg j"><label>Adresse de départ</label><input type="text" id="bv-dep-adr" placeholder="Adresse complète…"></div>
            <div class="fr">
              <div class="fg j"><label>Contact départ</label><input type="text" id="bv-dep-tel"></div>
              <div class="fg j"><label>Heure</label><input type="time" id="bv-dep-h"></div>
            </div>
          </div>
          <div class="bon-section">
            <div style="font-weight:700;color:var(--orng);margin-bottom:8px;">🏁 ARRIVÉE</div>
            <div class="fg j"><label>Adresse d'arrivée</label><input type="text" id="bv-arr-adr"></div>
            <div class="fr">
              <div class="fg j"><label>Contact arrivée</label><input type="text" id="bv-arr-tel"></div>
              <div class="fg j"><label>Heure</label><input type="time" id="bv-arr-h"></div>
            </div>
          </div>
          <div class="bon-section">
            <div style="font-weight:700;margin-bottom:8px;">🚗 VÉHICULE</div>
            <div class="fr3">
              <div class="fg j"><label>Marque / Modèle</label><input type="text" id="bv-modele"></div>
              <div class="fg j"><label>Immatriculation</label><input type="text" id="bv-immat"></div>
              <div class="fg j"><label>Couleur</label><input type="text" id="bv-couleur"></div>
            </div>
          </div>
          <div class="bon-section">
            <div style="font-weight:700;margin-bottom:8px;">✅ OPTIONS / INSTRUCTIONS</div>
            <div id="bv-options"></div>
            <button class="btn btn-g btn-sm no-print" onclick="ajouterOption('conv')" style="margin-top:6px;">+ Ajouter option</button>
            <div class="fg j" style="margin-top:10px;"><textarea id="bv-instruct" rows="3" placeholder="Instructions particulières…"></textarea></div>
          </div>
          <div style="background:var(--gris-l);padding:10px;border-radius:6px;font-size:11px;color:#888;text-align:center;">
            HEXAGONE CONFLUENCE — SIREN 102 530 596 000 12 — RCS Versailles
          </div>
        </div>
      </div>
      <button class="btn btn-v btn-w no-print" style="margin-top:12px;" onclick="imprimerBon()">🖨️ Générer PDF</button>
    </div>
  </div>
</div>

<!-- ═══════════════════════════════ BILAN ═══════════════════════════════ -->
<div id="page-bilan" class="page">
  <div style="font-size:17px;font-weight:700;margin-bottom:14px;">Bilan simplifié</div>
  <div class="bilan-grid">
    <div class="bilan-col card">
      <div class="bilan-head" style="background:var(--bleu);">ACTIF</div>
      <div id="bilan-actif"></div>
      <div class="bilan-total" style="background:var(--bleu-l);">
        <span>TOTAL ACTIF</span><span class="mt mp" id="bilan-total-actif">0 €</span>
      </div>
    </div>
    <div class="bilan-col card">
      <div class="bilan-head" style="background:var(--orng);">PASSIF</div>
      <div id="bilan-passif"></div>
      <div class="bilan-total" style="background:var(--orng-l);">
        <span>TOTAL PASSIF</span><span class="mt" id="bilan-total-passif" style="color:var(--orng);font-weight:700;">0 €</span>
      </div>
    </div>
  </div>

  <div class="card" style="margin-top:14px;">
    <div class="ch gris">Créances & Dettes (hors trésorerie)</div>
    <div class="cb">
      <div class="st">Créances clients</div>
      <div id="bilan-creances"></div>
      <button class="btn btn-g btn-sm" onclick="ajouterCreance()" style="margin-top:6px;">+ Ajouter créance</button>
      <div class="st">Dettes fournisseurs</div>
      <div id="bilan-dettes"></div>
      <button class="btn btn-g btn-sm" onclick="ajouterDette()" style="margin-top:6px;">+ Ajouter dette</button>
    </div>
  </div>
</div>

<!-- ═══════════════════════════════ RÉFÉRENCES ═══════════════════════════════ -->
<div id="page-refs" class="page">
  <div class="card">
    <div class="ch bleu">Clients <button class="btn-wh" onclick="openModal('modal-client')">+ Ajouter</button></div>
    <div class="tw">
      <table><thead><tr><th>Nom</th><th>Abrév.</th><th>Délai paiement</th><th></th></tr></thead>
      <tbody id="tb-clients"></tbody></table>
    </div>
  </div>
  <div class="card">
    <div class="ch orng">Convoyeurs <button class="btn-wh" onclick="openModal('modal-conv')">+ Ajouter</button></div>
    <div class="tw">
      <table><thead><tr><th>Nom</th><th>Statut</th><th>Délai</th><th>IBAN</th><th></th></tr></thead>
      <tbody id="tb-convs"></tbody></table>
    </div>
  </div>
  <div class="card">
    <div class="ch gris">Paramètres mois courant</div>
    <div class="cb">
      <div class="fr">
        <div class="fg j"><label>Mois (MM)</label><input type="text" id="p-mois" maxlength="2" placeholder="06"></div>
        <div class="fg j"><label>Année (AA)</label><input type="text" id="p-annee" maxlength="2" placeholder="26"></div>
      </div>
      <div class="fg j"><label>Solde initial banque (€)</label><input type="number" id="p-banque-init" step="0.01"></div>
      <div class="fg j"><label>Solde initial caisse (€)</label><input type="number" id="p-caisse-init" step="0.01"></div>
      <div class="fg j"><label>Dette CCA initiale (€)</label><input type="number" id="p-cca-init" step="0.01"></div>
      <div class="fg j"><label>Solde TVA initial (€)</label><input type="number" id="p-tva-init" step="0.01"></div>
      <button class="btn btn-p btn-w" onclick="sauverParams()">Enregistrer paramètres</button>
    </div>
  </div>

  <div class="card">
    <div class="ch gris">Import / Export données</div>
    <div class="cb" style="display:flex;gap:10px;flex-direction:column;">
      <button class="btn btn-p btn-w" onclick="exporterJSON()">📤 Exporter données (JSON)</button>
      <div class="fg"><label>Importer données (JSON)</label><input type="file" id="import-file" accept=".json" onchange="importerJSON(this)"></div>
    </div>
  </div>
</div>

<!-- ═══════════════════════════════ MODALS ═══════════════════════════════ -->

<!-- Modal Mission -->
<div id="modal-mission" class="modal-overlay">
<div class="modal">
  <div class="modal-handle"></div>
  <div class="modal-title">Nouvelle mission</div>
  <div class="fr">
    <div class="fg j"><label>Date</label><input type="date" id="m-date"></div>
    <div class="fg j"><label>Client</label><select id="m-client" onchange="majFacturesClient()"></select></div>
  </div>
  <div class="fg j"><label>Convoyeur</label><select id="m-convoyeur"></select></div>
  <div class="fg j">
    <label>Facture</label>
    <select id="m-facture">
      <option value="__new__">+ Nouvelle facture</option>
    </select>
  </div>
  <div class="fr">
    <div class="fg j"><label>Prix mission HT (€)</label><input type="number" id="m-ht" step="0.01" placeholder="0.00"></div>
    <div class="fg j"><label>Rémunération convoyeur HT (€)</label><input type="number" id="m-conv" step="0.01" placeholder="0.00"></div>
  </div>
  <div class="st">Dépenses TTC</div>
  <div class="fr3">
    <div class="fg j"><label>Carburant 20%</label><input type="number" id="m-carbu" step="0.01" placeholder="0.00"></div>
    <div class="fg j"><label>Péage 20%</label><input type="number" id="m-peage" step="0.01" placeholder="0.00"></div>
    <div class="fg j"><label>Lavage 20%</label><input type="number" id="m-lavage" step="0.01" placeholder="0.00"></div>
  </div>
  <div class="fr3">
    <div class="fg j"><label>Repas fast-food 10%</label><input type="number" id="m-ff" step="0.01" placeholder="0.00"></div>
    <div class="fg j"><label>Repas supermarché 5,5%</label><input type="number" id="m-sm" step="0.01" placeholder="0.00"></div>
    <div class="fg j"><label>Transport aller (non déd.)</label><input type="number" id="m-ta" step="0.01" placeholder="0.00"></div>
  </div>
  <div class="fr3">
    <div class="fg j"><label>Transport retour (non déd.)</label><input type="number" id="m-tr" step="0.01" placeholder="0.00"></div>
    <div class="fg j"><label>HT sans TVA</label><input type="number" id="m-hst" step="0.01" placeholder="0.00"></div>
    <div class="fg j"><label>Autre 20%</label><input type="number" id="m-autre" step="0.01" placeholder="0.00"></div>
  </div>
  <div class="fg j"><label>Lien justificatif (Drive)</label><input type="url" id="m-lien" placeholder="https://…"></div>
  <div style="display:flex;gap:8px;margin-top:8px;">
    <button class="btn btn-g btn-w" onclick="closeModal('modal-mission')">Annuler</button>
    <button class="btn btn-v btn-w" onclick="sauverMission()">Enregistrer</button>
  </div>
</div>
</div>

<!-- Modal Comptabilité -->
<div id="modal-compta" class="modal-overlay">
<div class="modal">
  <div class="modal-handle"></div>
  <div class="modal-title">Nouvelle écriture comptable</div>
  <div class="fr">
    <div class="fg j"><label>Date</label><input type="date" id="c-date"></div>
    <div class="fg j"><label>Type d'opération</label>
      <select id="c-type">
        <option value="">-- Choisir --</option>
        <option>Recette client</option><option>Recette especes</option>
        <option>Remboursement recu avec TVA</option><option>Remboursement recu sans TVA</option>
        <option>Note de frais</option><option>Paiement convoyeur</option>
        <option>Paiement TVA</option><option>Depense CB</option>
        <option>Depense especes</option><option>Depense virement</option>
        <option>Apport CCA</option><option>Remboursement CCA</option>
        <option>Retrait especes</option><option>Depot especes</option>
        <option>Ajustement TVA positif</option>
        <option>Ajustement TVA negatif</option>
        <option>Autre</option>
      </select>
    </div>
  </div>
  <div class="fr">
    <div class="fg j"><label>Catégorie</label>
      <select id="c-cat">
        <option value="">-- Choisir --</option>
        <option>Charge</option><option>Immobilisation</option><option>Recette client</option>
        <option>Recette especes</option><option>Remboursement recu</option>
        <option>Note de frais</option><option>Paiement convoyeur</option>
        <option>Paiement TVA</option><option>Apport CCA</option>
        <option>Remboursement CCA</option><option>Retrait especes</option>
        <option>Depense especes</option><option>Depot especes</option><option>Autre</option>
      </select>
    </div>
    <div class="fg j"><label>Sous-catégorie</label>
      <select id="c-scat">
        <option value="">-- Choisir --</option>
        <option>Carburant</option><option>Peage</option><option>Nourriture fast-food</option>
        <option>Nourriture supermarche</option><option>Hotel</option>
        <option>Transport (train/bus/tram)</option><option>Lavage vehicule</option>
        <option>Materiel bureau</option><option>Abonnement</option>
        <option>Telephone</option><option>Publicite</option>
        <option>Honoraires comptable</option><option>Assurance</option><option>Autre</option>
      </select>
    </div>
  </div>
  <div class="fr">
    <div class="fg j"><label>Convoyeur (si paiement)</label><select id="c-conv"><option value="">--</option></select></div>
    <div class="fg j"><label>Client (si recette)</label><select id="c-cli"><option value="">--</option></select></div>
  </div>
  <div class="fr">
    <div class="fg j"><label>Réf. Mission ou N° Facture</label><input type="text" id="c-ref" placeholder="FAC-2606-CC-001"></div>
    <div class="fg j"><label>Descriptif</label><input type="text" id="c-desc" placeholder="Description…"></div>
  </div>
  <div class="st">Montant TTC (saisir dans la bonne colonne)</div>
  <div class="fr">
    <div class="fg j"><label>Non déductible (€)</label><input type="number" id="c-nd" step="0.01" placeholder="0.00"></div>
    <div class="fg j"><label>5,5% supermarché (€)</label><input type="number" id="c-55" step="0.01" placeholder="0.00"></div>
  </div>
  <div class="fr">
    <div class="fg j"><label>10% fast-food (€)</label><input type="number" id="c-10" step="0.01" placeholder="0.00"></div>
    <div class="fg j"><label>20% carbu/péage/bureau (€)</label><input type="number" id="c-20" step="0.01" placeholder="0.00"></div>
  </div>
  <div class="fg j"><label>Sans TVA — CCA / espèces / TVA / convoyeur AE (€)</label><input type="number" id="c-st" step="0.01" placeholder="0.00"></div>
  <div class="fg j"><label>Lien justificatif</label><input type="url" id="c-lien" placeholder="https://…"></div>
  <div style="display:flex;gap:8px;margin-top:8px;">
    <button class="btn btn-g btn-w" onclick="closeModal('modal-compta')">Annuler</button>
    <button class="btn btn-p btn-w" onclick="sauverCompta()">Enregistrer</button>
  </div>
</div>
</div>

<!-- Modal Client -->
<div id="modal-client" class="modal-overlay">
<div class="modal">
  <div class="modal-handle"></div>
  <div class="modal-title">Nouveau client</div>
  <div class="fg j"><label>Nom du client</label><input type="text" id="cl-nom" placeholder="Convoyage&Co"></div>
  <div class="fg j"><label>Abréviation (unique)</label><input type="text" id="cl-abrev" placeholder="C&C"></div>
  <div class="fg j"><label>Délai de paiement</label>
    <select id="cl-delai"><option value="immediat">Immédiat</option><option value="15 jours">15 jours</option><option value="30 jours">30 jours</option><option value="fin de mois">Fin de mois</option></select>
  </div>
  <div style="display:flex;gap:8px;margin-top:8px;">
    <button class="btn btn-g btn-w" onclick="closeModal('modal-client')">Annuler</button>
    <button class="btn btn-p btn-w" onclick="sauverClient()">Enregistrer</button>
  </div>
</div>
</div>

<!-- Modal Convoyeur -->
<div id="modal-conv" class="modal-overlay">
<div class="modal">
  <div class="modal-handle"></div>
  <div class="modal-title">Nouveau convoyeur / membre</div>
  <div class="fg j"><label>Nom</label><input type="text" id="cv-nom" placeholder="Ibrahim"></div>
  <div class="fr">
    <div class="fg j"><label>Statut</label>
      <select id="cv-stat" onchange="majDelaiConv()">
        <option value="Gérant">Gérant (équipe interne — 0€)</option>
        <option value="AE">Auto-entrepreneur (AE)</option>
        <option value="SOC">Société (SOC)</option>
        <option value="CDI">Salarié CDI</option>
        <option value="CDD">Salarié CDD</option>
      </select>
    </div>
    <div class="fg j"><label>Délai de paiement</label><select id="cv-delai">
      <option value="quinzaine">À la quinzaine</option>
      <option value="mois">Au mois</option>
      <option value="30jfm">30 jours fin de mois</option>
      <option value="45jfm">45 jours fin de mois</option>
      <option value="immediat">Immédiat</option>
      <option value="na">N/A (interne)</option>
    </select></div>
  </div>
  <div class="fg j"><label>IBAN</label><input type="text" id="cv-iban" placeholder="FR76…"></div>
  <div style="display:flex;gap:8px;margin-top:8px;">
    <button class="btn btn-g btn-w" onclick="closeModal('modal-conv')">Annuler</button>
    <button class="btn btn-o btn-w" onclick="sauverConv()">Enregistrer</button>
  </div>
</div>
</div>

<!-- Modal Mois -->
<div id="modal-mois" class="modal-overlay">
<div class="modal">
  <div class="modal-handle"></div>
  <div class="modal-title">Changer de mois</div>
  <p style="font-size:13px;color:#666;margin-bottom:14px;">Sélectionner le mois de travail. Les données sont conservées pour tous les mois.</p>
  <div id="liste-mois-disponibles"></div>
  <button class="btn btn-p btn-w" style="margin-top:12px;" onclick="nouveauMois()">+ Nouveau mois</button>
</div>
</div>

<!-- Modal Récurrences -->
<div id="modal-recurrentes" class="modal-overlay">
<div class="modal">
  <div class="modal-handle"></div>
  <div class="modal-title">🔄 Écritures récurrentes</div>
  <p style="font-size:12px;color:#888;margin-bottom:12px;">Ces écritures apparaissent comme rappels le jour J. Elles ne sont comptabilisées que quand vous les confirmez.</p>
  <div id="liste-recurrentes"></div>
  <div class="st" style="margin-top:14px;">Nouvelle récurrence</div>
  <div class="fg j"><label>Description</label><input type="text" id="rec-desc" placeholder="Abonnement Claude, Loyer, etc."></div>
  <div class="fr">
    <div class="fg j"><label>Type</label>
      <select id="rec-type">
        <option>Depense CB</option><option>Depense virement</option>
        <option>Depense especes</option><option>Abonnement</option>
      </select>
    </div>
    <div class="fg j"><label>Jour du mois</label><input type="number" id="rec-jour" min="1" max="31" placeholder="1"></div>
  </div>
  <div class="fr">
    <div class="fg j"><label>Montant TTC (€)</label><input type="number" id="rec-montant" step="0.01" placeholder="0.00"></div>
    <div class="fg j"><label>Colonne TVA</label>
      <select id="rec-col">
        <option value="t20">20% (CB/virement standard)</option>
        <option value="t10">10% (fast-food)</option>
        <option value="t55">5,5% (supermarché)</option>
        <option value="nd">Non déductible</option>
        <option value="st">Sans TVA</option>
      </select>
    </div>
  </div>
  <div style="display:flex;gap:8px;margin-top:8px;">
    <button class="btn btn-g btn-w" onclick="closeModal('modal-recurrentes')">Fermer</button>
    <button class="btn btn-p btn-w" onclick="sauverRecurrente()">Ajouter</button>
  </div>
</div>
</div>

<div id="toast" class="toast"></div>

<script>
// ═══════════════════════════════════════════════════════════════════
// CONSTANTES
// ═══════════════════════════════════════════════════════════════════
const OPTIONS_LIST = [
  'Remise carburant','Certificat W garage','Lavage exterieur','Lavage interieur',
  'Lavage complet','Lavage premium','Mise en main confort','Plein de carburant',
  'Rapport etat vehicule','Livraison express'
];

// ═══════════════════════════════════════════════════════════════════
// STATE
// ═══════════════════════════════════════════════════════════════════
// Mois courant auto selon date réelle
const _now = new Date();
const _moisAuto = String(_now.getMonth()+1).padStart(2,'0') + String(_now.getFullYear()).slice(2);

let state = {
  moisCourant: _moisAuto,
  clients: [],
  convoyeurs: [],
  mois: {},
  bilanManuel: { creances:[], dettes:[] }
};

function getMois() {
  if (!state.mois[state.moisCourant]) {
    state.mois[state.moisCourant] = { missions:[], compta:[], params:{ banqueInit:0, caisseInit:0, ccaInit:0, tvaInit:0 } };
  }
  return state.mois[state.moisCourant];
}

function getData() { return getMois(); }

// ═══════════════════════════════════════════════════════════════════
// PERSISTENCE
// ═══════════════════════════════════════════════════════════════════
function chargerState() {
  try {
    const s = localStorage.getItem('hcg_state_v2');
    if (s) state = JSON.parse(s);
  } catch(e){}
  if (!state.clients || state.clients.length===0) {
    state.clients=[
      {id:1,nom:'Convoyage&Co',abrev:'C&C',delai:'30 jours'},
      {id:2,nom:'Hexagone Confluence',abrev:'HCF',delai:'fin de mois'},
    ];
    state.convoyeurs=[
      {id:1,nom:'Ibrahim',statut:'AE',delai:'15 jours',iban:''},
      {id:2,nom:'Zineddine',statut:'AE',delai:'15 jours',iban:''},
      {id:3,nom:'Amin',statut:'AE',delai:'15 jours',iban:''},
    ];
    state.moisCourant=_moisAuto;
    state.mois={};
    state.bilanManuel={creances:[],dettes:[]};
    sauverState();
  }
}

function sauverState() {
  localStorage.setItem('hcg_state_v2', JSON.stringify(state));
}

// ═══════════════════════════════════════════════════════════════════
// NAVIGATION
// ═══════════════════════════════════════════════════════════════════
function showPage(page, tabEl) {
  document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));
  document.querySelectorAll('.nav-tab').forEach(t=>t.classList.remove('active'));
  document.getElementById('page-'+page).classList.add('active');
  if(tabEl) tabEl.classList.add('active');
  refreshPage(page);
}

function refreshPage(page) {
  const fns = {
    dashboard: refreshDash, missions: refreshMissions, compta: refreshCompta,
    convoyeurs: refreshConvoyeurs, clients: refreshClients,
    bons: refreshBons, bilan: refreshBilan, refs: refreshRefs
  };
  if(fns[page]) fns[page]();
}

// ═══════════════════════════════════════════════════════════════════
// CALCULS
// ═══════════════════════════════════════════════════════════════════
function eur(n) {
  return (n||0).toLocaleString('fr-FR',{minimumFractionDigits:2,maximumFractionDigits:2})+' €';
}

function calcMission(m) {
  const dep20 = (m.carbu||0)+(m.peage||0)+(m.lavage||0)+(m.autre||0);
  const dep10 = m.ff||0;
  const dep55 = m.sm||0;
  const depND = (m.ta||0)+(m.tr||0)+(m.hst||0);
  const depTTC = dep20+dep10+dep55+depND;
  const depHT  = dep20/1.2+dep10/1.1+dep55/1.055+depND;
  const benef  = (m.ht||0)-depHT-(m.conv||0);
  const tvaC   = (m.ht||0)*0.2;
  const tvaD   = dep20/1.2*0.2+dep10/1.1*0.1+dep55/1.055*0.055;
  return {depTTC,depHT,benef,tvaC,tvaD};
}

function calcSoldes() {
  const d = getData();
  const p = d.params||{};
  let banque=p.banqueInit||0, caisse=p.caisseInit||0, cca=p.ccaInit||0, tva=p.tvaInit||0;
  let tvaColl=0, tvaDed=0;

  const ecrs=[...d.compta].sort((a,b)=>new Date(a.date)-new Date(b.date));
  const ENTREES=['Recette client','Recette especes','Apport CCA','Depot especes',
    'Remboursement recu avec TVA','Remboursement recu sans TVA','Note de frais'];
  const NEUTRES=['Depense especes'];

  for(const e of ecrs){
    const tot=(e.nd||0)+(e.t55||0)+(e.t10||0)+(e.t20||0)+(e.st||0);
    const tvaC55=e.t55-(e.t55/1.055); const tvaC10=e.t10-(e.t10/1.1); const tvaC20=e.t20-(e.t20/1.2);
    const tvaLoc=tvaC55+tvaC10+tvaC20;

    if(ENTREES.includes(e.type)) banque+=tot;
    else if(!NEUTRES.includes(e.type)) banque-=tot;
    if(e.type==='Retrait especes'){banque-=tot;caisse+=tot;}
    else if(e.type==='Depense especes') caisse-=tot;
    else if(e.type==='Depot especes') caisse-=tot;
    if(e.type==='Apport CCA'||e.type==='Note de frais') cca+=e.st||0;
    if(e.type==='Remboursement CCA') cca-=e.st||0;
    if(['Recette client','Recette especes'].includes(e.type)){tvaColl+=tvaLoc;tva+=tvaLoc;}
    if(['Depense CB','Depense especes','Depense virement','Note de frais'].includes(e.type)){tvaDed+=tvaLoc;tva-=tvaLoc;}
    if(e.type==='Paiement TVA'){tva-=(e.st||0);}
    if(e.type==='Ajustement TVA positif'){tva+=(e.st||0);}
    if(e.type==='Ajustement TVA negatif'){tva-=(e.st||0);}
    e._b=banque; e._ca=caisse; e._cca=cca; e._tva=tva; e._tc=tvaLoc; e._td=tvaLoc;
  }
  return {banque,caisse,cca,tva,tvaColl,tvaDed};
}

function isEncaissee(m) {
  const d=getData();
  if(!m.numFac) return false;
  return d.compta.some(e=>['Recette client','Recette especes'].includes(e.type)&&e.ref===m.numFac);
}

function convoyeurPaye(m) {
  const d=getData();
  return d.compta.some(e=>e.type==='Paiement convoyeur'&&e.convId===m.convoyeurId&&e.ref===m.codeMission);
}

// ═══════════════════════════════════════════════════════════════════
// DASHBOARD
// ═══════════════════════════════════════════════════════════════════
function refreshDash() {
  const d=getData(); const s=calcSoldes();
  const totCA=d.missions.reduce((a,m)=>a+(m.ht||0),0);
  const totBen=d.missions.reduce((a,m)=>a+calcMission(m).benef,0);
  const convDu=d.missions.reduce((a,m)=>a+(m.conv||0),0);
  const convNP=d.missions.filter(m=>m.conv>0&&!convoyeurPaye(m)).length;
  const cliAtt=state.clients.filter(cl=>d.missions.filter(m=>m.clientId===cl.id&&!isEncaissee(m)).length>0);
  const totAtt=cliAtt.reduce((a,cl)=>a+d.missions.filter(m=>m.clientId===cl.id&&!isEncaissee(m)).reduce((b,m)=>b+(m.ht||0),0),0);

  document.getElementById('k-ca').textContent=eur(totCA);
  document.getElementById('k-nb-miss').textContent=d.missions.length+' mission(s)';
  document.getElementById('k-banque').textContent=eur(s.banque);
  document.getElementById('k-conv').textContent=eur(convDu);
  document.getElementById('k-conv-nb').textContent=convNP+' non payé(s)';
  document.getElementById('k-tva').textContent=eur(s.tva);
  document.getElementById('k-enc').textContent=eur(totAtt);
  document.getElementById('k-enc-nb').textContent=cliAtt.length+' client(s)';
  document.getElementById('k-caisse').textContent=eur(s.caisse);
  document.getElementById('k-benef').textContent=eur(totBen);
  document.getElementById('k-cca').textContent=eur(s.cca);

  // Dernières missions
  const dm=document.getElementById('dash-missions');
  if(!d.missions.length){dm.innerHTML='<div class="empty"><div class="ei">📋</div><div class="et">Aucune mission</div></div>';return;}
  dm.innerHTML=d.missions.slice(-4).reverse().map(m=>{
    const cl=state.clients.find(c=>c.id===m.clientId);
    const enc=isEncaissee(m);
    return`<div style="display:flex;justify-content:space-between;align-items:center;padding:10px 14px;border-bottom:1px solid var(--border);">
      <div><span class="cm">${m.codeMission||'—'}</span><span style="font-size:12px;color:#666;margin-left:6px;">${cl?cl.nom:'—'} • ${m.date||'—'}</span></div>
      <div style="text-align:right;"><div style="font-weight:700;">${eur(m.ht)}</div><span class="badge ${enc?'bv':'br'}">${enc?'Encaissé':'En attente'}</span></div>
    </div>`;
  }).join('');

  // Convoyeurs à payer
  const dc=document.getElementById('dash-conv');
  const convNPList=d.missions.filter(m=>m.conv>0&&!convoyeurPaye(m));
  if(!convNPList.length){dc.innerHTML='<div class="empty"><div class="ei">✅</div><div class="et">Tous à jour</div></div>';} 
  else {
    const byConv={};
    convNPList.forEach(m=>{
      const cv=state.convoyeurs.find(c=>c.id===m.convoyeurId);
      const n=cv?cv.nom:'?';
      byConv[n]=(byConv[n]||0)+(m.conv||0);
    });
    dc.innerHTML=Object.entries(byConv).map(([n,v])=>`<div style="display:flex;justify-content:space-between;padding:10px 14px;border-bottom:1px solid var(--border);">
      <span style="font-weight:600;">🚗 ${n}</span><span class="mn">${eur(v)}</span></div>`).join('');
  }

  // Clients en attente
  const dl=document.getElementById('dash-cli');
  if(!cliAtt.length){dl.innerHTML='<div class="empty"><div class="ei">✅</div><div class="et">Tous ont payé</div></div>';}
  else {
    dl.innerHTML=cliAtt.map(cl=>{
      const att=d.missions.filter(m=>m.clientId===cl.id&&!isEncaissee(m)).reduce((a,m)=>a+(m.ht||0),0);
      return`<div style="display:flex;justify-content:space-between;padding:10px 14px;border-bottom:1px solid var(--border);">
        <span style="font-weight:600;">🏢 ${cl.nom}</span><span class="mn">${eur(att)}</span></div>`;
    }).join('');
  }
}

// ═══════════════════════════════════════════════════════════════════
// MISSIONS
// ═══════════════════════════════════════════════════════════════════
function refreshMissions() {
  const d=getData();
  document.getElementById('miss-count').textContent=d.missions.length+' mission(s) ce mois';
  let tCA=0,tDep=0,tConv=0,tBen=0,tTvaC=0,tTvaD=0;

  const tbody=document.getElementById('tbody-miss');
  if(!d.missions.length){
    tbody.innerHTML='<tr><td colspan="25" style="text-align:center;padding:30px;color:#aaa;">Aucune mission</td></tr>';
  } else {
    tbody.innerHTML=d.missions.map((m,i)=>{
      const cl=state.clients.find(c=>c.id===m.clientId);
      const cv=state.convoyeurs.find(c=>c.id===m.convoyeurId);
      const st=calcMission(m);
      const enc=isEncaissee(m);
      tCA+=m.ht||0;tDep+=st.depHT;tConv+=m.conv||0;tBen+=st.benef;tTvaC+=st.tvaC;tTvaD+=st.tvaD;
      return`<tr class="${enc?'row-v':'row-r'}">
        <td>${m.date||'—'}</td>
        <td><span class="cm">${m.codeMission||'—'}</span></td>
        <td style="font-size:10px;">${m.numFac||'—'}</td>
        <td>${cl?cl.nom:'—'}</td>
        <td>${cv?cv.nom:'—'}</td>
        <td class="mt">${eur(m.ht)}</td>
        <td class="mt">${eur((m.ht||0)*1.2)}</td>
        <td class="mt">${eur(m.carbu)}</td>
        <td class="mt">${eur(m.peage)}</td>
        <td class="mt">${eur(m.lavage)}</td>
        <td class="mt">${eur(m.ff)}</td>
        <td class="mt">${eur(m.sm)}</td>
        <td class="mt">${eur(m.ta)}</td>
        <td class="mt">${eur(m.tr)}</td>
        <td class="mt">${eur(m.hst)}</td>
        <td class="mt">${eur(m.autre)}</td>
        <td class="mt mn">${eur(st.depTTC)}</td>
        <td class="mt mn">${eur(st.depHT)}</td>
        <td class="mt mn">${eur(m.conv)}</td>
        <td class="mt ${st.benef>=0?'mp':'mn'}">${eur(st.benef)}</td>
        <td class="mt" style="color:var(--tva);">${eur(st.tvaC)}</td>
        <td class="mt" style="color:var(--vert);">${eur(st.tvaD)}</td>
        <td><span class="badge ${enc?'bv':'br'}">${enc?'Encaissé':'En attente'}</span></td>
        <td>${m.lien?`<a href="${m.lien}" target="_blank" style="font-size:11px;">🔗</a>`:'—'}</td>
        <td style="display:flex;gap:4px;">
          <button class="btn btn-g btn-sm" onclick="modifierMission(${i})">✏️</button>
          <button class="btn btn-r btn-sm" onclick="suppMission(${i})">🗑</button>
        </td>
      </tr>`;
    }).join('');
  }
  document.getElementById('tot-ca').textContent=eur(tCA);
  document.getElementById('tot-dep').textContent=eur(tDep);
  document.getElementById('tot-conv').textContent=eur(tConv);
  document.getElementById('tot-ben').textContent=eur(tBen);
  document.getElementById('tot-tva-c').textContent=eur(tTvaC);
  document.getElementById('tot-tva-d').textContent=eur(tTvaD);
}

function majFacturesClient(){
  const cliId=parseInt(document.getElementById('m-client').value);
  const sel=document.getElementById('m-facture');
  sel.innerHTML='<option value="__new__">+ Nouvelle facture</option>';
  if(!cliId) return;
  const cl=state.clients.find(c=>c.id===cliId);
  if(!cl) return;
  // Récupérer toutes les factures existantes de ce client tous mois confondus
  const facsExist=new Set();
  Object.values(state.mois).forEach(md=>{
    (md.missions||[]).filter(m=>m.clientId===cliId).forEach(m=>{if(m.numFac)facsExist.add(m.numFac);});
  });
  facsExist.forEach(f=>{
    const o=document.createElement('option');o.value=f;o.textContent=f;sel.appendChild(o);
  });
}

function sauverMission() {
  const dateVal=document.getElementById('m-date').value;
  if(!dateVal){showToast('⚠️ Date requise');return;}

  // Déterminer le mois cible selon la DATE de la mission
  const d=new Date(dateVal);
  const moisCible=String(d.getMonth()+1).padStart(2,'0')+String(d.getFullYear()).slice(2);

  // Créer le mois si inexistant
  if(!state.mois[moisCible]) state.mois[moisCible]={missions:[],compta:[],params:{banqueInit:0,caisseInit:0,ccaInit:0,tvaInit:0}};
  const dm=state.mois[moisCible];

  const cliId=parseInt(document.getElementById('m-client').value);
  if(!cliId){showToast('⚠️ Choisir un client');return;}
  const cl=state.clients.find(c=>c.id===cliId);

  // Logique facture : existante ou nouvelle
  const facSel=document.getElementById('m-facture').value;
  let fac, mc;
  if(facSel==='__new__'){
    const allFacs=new Set();
    Object.values(state.mois).forEach(md=>{
      (md.missions||[]).filter(m=>m.clientId===cliId).forEach(m=>{if(m.numFac)allFacs.add(m.numFac);});
    });
    const nb=allFacs.size+1;
    fac=cl?'FAC-'+cl.abrev+'-'+String(nb).padStart(3,'0'):'FAC-???';
  } else {
    fac=facSel;
  }

  // Code mission basé sur le mois cible
  const nbMois=dm.missions.filter(m=>m.clientId===cliId).length+1;
  mc=cl?cl.abrev+'-'+moisCible+'-'+String(nbMois).padStart(3,'0'):'???';

  const convId=parseInt(document.getElementById('m-convoyeur').value)||null;
  const convObj=state.convoyeurs.find(c=>c.id===convId);
  const isInterne=convObj&&['Gérant','CDI','CDD'].includes(convObj.statut);
  const convRem=isInterne?0:n('m-conv');

  const m={
    id:Date.now(),date:dateVal,
    clientId:cliId,convoyeurId:convId,
    codeMission:mc,numFac:fac,
    ht:n('m-ht'),conv:convRem,carbu:n('m-carbu'),peage:n('m-peage'),
    lavage:n('m-lavage'),ff:n('m-ff'),sm:n('m-sm'),ta:n('m-ta'),
    tr:n('m-tr'),hst:n('m-hst'),autre:n('m-autre'),lien:document.getElementById('m-lien').value,
  };

  // Avertissement si mois différent du mois courant
  if(moisCible!==state.moisCourant){
    const moisNoms=['Jan','Fév','Mar','Avr','Mai','Jun','Jul','Aoû','Sep','Oct','Nov','Déc'];
    const ml=moisNoms[d.getMonth()]+' 20'+moisCible.slice(2);
    showToast('✅ Mission créée → rangée en '+ml);
  } else {
    showToast('✅ Mission '+mc+' — Facture '+fac);
  }

  dm.missions.push(m);sauverState();closeModal('modal-mission');
  refreshMissions();refreshDash();
}

function suppMission(i){
  if(!confirm('Supprimer ?'))return;
  getData().missions.splice(i,1);sauverState();refreshMissions();refreshDash();showToast('Mission supprimée');
}

// ═══════════════════════════════════════════════════════════════════
// COMPTABILITÉ
// ═══════════════════════════════════════════════════════════════════
function refreshCompta() {
  const d=getData();const s=calcSoldes();
  document.getElementById('c-banque').textContent=eur(s.banque);
  document.getElementById('c-caisse').textContent=eur(s.caisse);
  document.getElementById('c-cca').textContent=eur(s.cca);
  document.getElementById('c-tva').textContent=eur(s.tva);
  document.getElementById('r-tva-c').textContent=eur(s.tvaColl);
  document.getElementById('r-tva-d').textContent=eur(s.tvaDed);
  document.getElementById('r-tva-n').textContent=eur(s.tva);

  const ecrs=[...d.compta].sort((a,b)=>new Date(a.date)-new Date(b.date));
  const tbody=document.getElementById('tbody-compta');
  if(!ecrs.length){tbody.innerHTML='<tr><td colspan="26" style="text-align:center;padding:30px;color:#aaa;">Aucune écriture</td></tr>';return;}

  const ENTREES=['Recette client','Recette especes','Apport CCA','Depot especes','Remboursement recu avec TVA','Remboursement recu sans TVA','Note de frais'];
  tbody.innerHTML=ecrs.map((e,i)=>{
    const tot=(e.nd||0)+(e.t55||0)+(e.t10||0)+(e.t20||0)+(e.st||0);
    const ht=(e.nd||0)+(e.t55||0)/1.055+(e.t10||0)/1.1+(e.t20||0)/1.2+(e.st||0);
    const tv55=e.t55-(e.t55||0)/1.055; const tv10=e.t10-(e.t10||0)/1.1; const tv20=e.t20-(e.t20||0)/1.2;
    const tvaC=['Recette client','Recette especes'].includes(e.type)?(tv55+tv10+tv20):0;
    const tvaD=['Depense CB','Depense especes','Depense virement','Note de frais'].includes(e.type)?(tv55+tv10+tv20):0;
    const isE=ENTREES.includes(e.type);
    const cv=state.convoyeurs.find(c=>c.id===e.convId);
    const cl=state.clients.find(c=>c.id===e.cliId);
    return`<tr class="${isE?'row-v':''}">
      <td>${e.date||'—'}</td>
      <td><span class="badge ${isE?'bv':'br'}" style="font-size:9px;">${e.type||'—'}</span></td>
      <td style="font-size:10px;">${e.cat||'—'}</td>
      <td style="font-size:10px;">${e.scat||'—'}</td>
      <td style="font-size:11px;">${cv?cv.nom:'—'}</td>
      <td style="font-size:11px;">${cl?cl.nom:'—'}</td>
      <td style="font-size:10px;" class="cm">${e.ref||'—'}</td>
      <td style="font-size:11px;">${e.desc||'—'}</td>
      <td class="mt">${e.nd?eur(e.nd):'—'}</td>
      <td class="mt">${e.t55?eur(e.t55):'—'}</td>
      <td class="mt">${e.t10?eur(e.t10):'—'}</td>
      <td class="mt">${e.t20?eur(e.t20):'—'}</td>
      <td class="mt">${e.st?eur(e.st):'—'}</td>
      <td class="mt ${isE?'mp':'mn'}">${eur(tot)}</td>
      <td class="mt">${eur(ht)}</td>
      <td class="mt" style="font-size:10px;">${tv55>0?eur(tv55):'—'}</td>
      <td class="mt" style="font-size:10px;">${tv10>0?eur(tv10):'—'}</td>
      <td class="mt" style="font-size:10px;">${tv20>0?eur(tv20):'—'}</td>
      <td class="mt" style="color:var(--tva);font-size:11px;">${tvaC>0?eur(tvaC):'—'}</td>
      <td class="mt" style="color:var(--vert);font-size:11px;">${tvaD>0?eur(tvaD):'—'}</td>
      <td class="mt" style="font-size:11px;">${eur(e._b||0)}</td>
      <td class="mt" style="font-size:11px;">${eur(e._ca||0)}</td>
      <td class="mt" style="font-size:11px;">${eur(e._cca||0)}</td>
      <td class="mt" style="font-size:11px;">${eur(e._tva||0)}</td>
      <td>${e.lien
        ? `<a href="${e.lien}" target="_blank" style="font-size:11px;">🔗</a>`
        : e.justifExpl
          ? `<span style="font-size:10px;color:#888;" title="${e.justifExpl}">📝 ${e.justifExpl.slice(0,15)}…</span>`
          : `<button class="btn btn-sm" style="background:#FFF3CD;color:#856404;border:1px solid #FFEAA7;font-size:10px;" onclick="ajouterJustif(${i})">⚠️ Justif.</button>`
      }</td>
      <td style="display:flex;gap:4px;">
        <button class="btn btn-g btn-sm" onclick="modifierCompta(${i})">✏️</button>
        <button class="btn btn-r btn-sm" onclick="suppCompta(${i})">🗑</button>
      </td>
    </tr>`;
  }).join('');
}

function sauverCompta() {
  const e={
    id:Date.now(),date:document.getElementById('c-date').value,
    type:document.getElementById('c-type').value,
    cat:document.getElementById('c-cat').value,
    scat:document.getElementById('c-scat').value,
    convId:parseInt(document.getElementById('c-conv').value)||null,
    cliId:parseInt(document.getElementById('c-cli').value)||null,
    ref:document.getElementById('c-ref').value,
    desc:document.getElementById('c-desc').value,
    nd:n('c-nd'),t55:n('c-55'),t10:n('c-10'),t20:n('c-20'),st:n('c-st'),
    lien:document.getElementById('c-lien').value,
  };
  if(!e.date||!e.type){showToast('⚠️ Date et type requis');return;}
  getData().compta.push(e);sauverState();closeModal('modal-compta');
  refreshCompta();refreshDash();showToast('✅ Écriture enregistrée');
}

function suppCompta(i){
  if(!confirm('Supprimer ?'))return;
  getData().compta.splice(i,1);sauverState();refreshCompta();refreshDash();showToast('Supprimée');
}

// ═══════════════════════════════════════════════════════════════════
// CONVOYEURS — avec accordion historique multi-mois
// ═══════════════════════════════════════════════════════════════════
function refreshConvoyeurs() {
  const d=getData();
  const el=document.getElementById('liste-conv');
  const cvAvec=state.convoyeurs.filter(cv=>Object.values(state.mois).some(md=>md.missions&&md.missions.some(m=>m.convoyeurId===cv.id&&m.conv>0)));

  if(!cvAvec.length){el.innerHTML='<div class="empty"><div class="ei">🚗</div><div class="et">Aucun convoyeur avec missions</div></div>';return;}

  el.innerHTML=cvAvec.map((cv,ci)=>{
    // Calcul tous les mois
    const moisData=Object.entries(state.mois).map(([mk,md])=>{
      const miss=(md.missions||[]).filter(m=>m.convoyeurId===cv.id&&m.conv>0);
      if(!miss.length)return null;
      const totDu=miss.reduce((a,m)=>a+(m.conv||0),0);
      const totVerse=(md.compta||[]).filter(e=>e.type==='Paiement convoyeur'&&e.convId===cv.id).reduce((a,e)=>a+(e.st||0),0);
      const mLabel=mk.slice(0,2)+'/20'+mk.slice(2);
      return{mk,mLabel,miss,totDu,totVerse,reste:totDu-totVerse};
    }).filter(Boolean).sort((a,b)=>a.mk>b.mk?-1:1);

    const totDuAll=moisData.reduce((a,m)=>a+(m.totDu),0);
    const totVerseAll=moisData.reduce((a,m)=>a+(m.totVerse),0);
    const resteAll=totDuAll-totVerseAll;

    return`<div class="card">
      <div class="accord-item ${resteAll>0?'':''}">
        <div class="accord-header" onclick="toggleAccord(this)">
          <div>
            <div style="font-weight:700;">🚗 ${cv.nom} <span class="badge ${cv.statut==='AE'?'bv':'bb'}" style="margin-left:6px;">${cv.statut}</span></div>
            <div style="font-size:12px;color:#666;margin-top:2px;">Délai : ${cv.delai}</div>
          </div>
          <div style="text-align:right;">
            <div class="mt ${resteAll>0?'mn':'mp'}" style="font-size:17px;font-weight:800;">${eur(resteAll)}</div>
            <span class="badge ${resteAll>0?'br':'bv'}">${resteAll>0?'Reste dû':'À jour'}</span>
            <div class="accord-arrow">▼</div>
          </div>
        </div>
        <div class="accord-body">
          ${moisData.map(md=>`
            <div style="margin-bottom:14px;">
              <div style="font-weight:700;color:var(--bleu);margin-bottom:8px;padding:6px 8px;background:var(--bleu-l);border-radius:6px;">
                📅 ${md.mLabel} — Dû: ${eur(md.totDu)} | Versé: ${eur(md.totVerse)} | <span class="${md.reste>0?'mn':'mp'}">${eur(md.reste)}</span>
              </div>
              <div class="tw">
                <table>
                  <thead><tr><th>Code</th><th>Date</th><th>Client</th><th>Rémun. HT</th><th>Échéance</th><th>Statut</th></tr></thead>
                  <tbody>
                  ${md.miss.map(m=>{
                    const cl=state.clients.find(c=>c.id===m.clientId);
                    const paye=(state.mois[md.mk].compta||[]).some(e=>e.type==='Paiement convoyeur'&&e.convId===cv.id&&e.ref===m.codeMission);
                    const ech=m.date?calcEcheanceConv(m.date,cv.delai||'quinzaine'):null;
                    const echStr=ech?ech.toLocaleDateString('fr-FR'):'—';
                    const st=(!paye&&ech)?statutEcheance(ech):{statut:'ok'};
                    return`<tr class="${paye?'row-v':'row-r'}">
                      <td><span class="cm">${m.codeMission||'—'}</span></td>
                      <td>${m.date||'—'}</td>
                      <td>${cl?cl.nom:'—'}</td>
                      <td class="mt ${paye?'mp':'mn'}">${eur(m.conv)}</td>
                      <td style="font-size:11px;">${echStr}</td>
                      <td>${paye
                        ?'<span class="badge bv">Payé</span>'
                        :st.statut==='rouge'
                          ?`<span class="badge br">🔴 ${st.label}</span>`
                          :st.statut==='orange'
                            ?`<span style="display:inline-block;padding:2px 7px;border-radius:10px;font-size:10px;font-weight:700;background:#FFF3CD;color:#856404;">🟠 ${st.label}</span>`
                            :'<span class="badge bg">En attente</span>'
                      }</td>
                    </tr>`;
                  }).join('')}
                  </tbody>
                </table>
              </div>
            </div>
          `).join('')}
        </div>
      </div>
    </div>`;
  }).join('');
}

// ═══════════════════════════════════════════════════════════════════
// CLIENTS — avec accordion historique factures
// ═══════════════════════════════════════════════════════════════════
function refreshClients() {
  const el=document.getElementById('liste-cli');
  const clAvec=state.clients.filter(cl=>Object.values(state.mois).some(md=>md.missions&&md.missions.some(m=>m.clientId===cl.id)));

  if(!clAvec.length){el.innerHTML='<div class="empty"><div class="ei">🏢</div><div class="et">Aucun client avec missions</div></div>';return;}

  el.innerHTML=clAvec.map(cl=>{
    const moisData=Object.entries(state.mois).map(([mk,md])=>{
      const miss=(md.missions||[]).filter(m=>m.clientId===cl.id);
      if(!miss.length)return null;
      const totDu=miss.reduce((a,m)=>a+(m.ht||0),0);
      const totEnc=(md.compta||[]).filter(e=>['Recette client','Recette especes'].includes(e.type)&&e.cliId===cl.id).reduce((a,e)=>{
        const ht=(e.nd||0)+(e.t55||0)/1.055+(e.t10||0)/1.1+(e.t20||0)/1.2+(e.st||0);
        return a+ht;
      },0);
      const mLabel=mk.slice(0,2)+'/20'+mk.slice(2);
      return{mk,mLabel,miss,totDu,totEnc,reste:totDu-totEnc,compta:md.compta||[]};
    }).filter(Boolean).sort((a,b)=>a.mk>b.mk?-1:1);

    const resteAll=moisData.reduce((a,m)=>a+(m.reste),0);

    return`<div class="card">
      <div class="accord-item">
        <div class="accord-header" onclick="toggleAccord(this)">
          <div>
            <div style="font-weight:700;">🏢 ${cl.nom} <span class="cm" style="margin-left:6px;">${cl.abrev}</span></div>
            <div style="font-size:12px;color:#666;margin-top:2px;">Délai : ${cl.delai}</div>
          </div>
          <div style="text-align:right;">
            <div class="mt ${resteAll>0?'mn':'mp'}" style="font-size:17px;font-weight:800;">${eur(resteAll)}</div>
            <span class="badge ${resteAll>0?'br':'bv'}">${resteAll>0?'En attente':'À jour'}</span>
            <div class="accord-arrow">▼</div>
          </div>
        </div>
        <div class="accord-body">
          ${moisData.map(md=>`
            <div style="margin-bottom:14px;">
              <div style="font-weight:700;color:var(--bleu);margin-bottom:8px;padding:6px 8px;background:var(--bleu-l);border-radius:6px;">
                📅 ${md.mLabel} — Dû HT: ${eur(md.totDu)} | Encaissé: ${eur(md.totEnc)} | <span class="${md.reste>0?'mn':'mp'}">${eur(md.reste)}</span>
              </div>
              <div class="tw">
                <table>
                  <thead><tr><th>N° Facture</th><th>Code</th><th>Date</th><th>Prix HT</th><th>Statut</th></tr></thead>
                  <tbody>
                  ${md.miss.map(m=>{
                    const enc=(md.compta).some(e=>['Recette client','Recette especes'].includes(e.type)&&e.ref===m.numFac);
                    return`<tr class="${enc?'row-v':'row-r'}">
                      <td style="font-size:10px;">${m.numFac||'—'}</td>
                      <td><span class="cm">${m.codeMission||'—'}</span></td>
                      <td>${m.date||'—'}</td>
                      <td class="mt ${enc?'mp':'mn'}">${eur(m.ht)}</td>
                      <td><span class="badge ${enc?'bv':'br'}">${enc?'Encaissé':'En attente'}</span></td>
                    </tr>`;
                  }).join('')}
                  </tbody>
                </table>
              </div>
            </div>
          `).join('')}
        </div>
      </div>
    </div>`;
  }).join('');
}

// ═══════════════════════════════════════════════════════════════════
// BONS
// ═══════════════════════════════════════════════════════════════════
function refreshBons() {
  const d=getData();
  ['bon-miss-sel','bon-conv-sel'].forEach(id=>{
    const sel=document.getElementById(id);
    sel.innerHTML='<option value="">-- Choisir une mission --</option>';
    d.missions.forEach((m,i)=>{
      const opt=document.createElement('option');
      opt.value=i;
      const cl=state.clients.find(c=>c.id===m.clientId);
      opt.textContent=(m.codeMission||'—')+' — '+(cl?cl.nom:'—')+' — '+(m.date||'—');
      sel.appendChild(opt);
    });
  });
}

function switchBon(type,tab) {
  document.querySelectorAll('.inner-tab').forEach(t=>t.classList.remove('active'));
  tab.classList.add('active');
  document.getElementById('bon-client-panel').style.display=type==='client'?'':'none';
  document.getElementById('bon-conv-panel').style.display=type==='convoyeur'?'':'none';
}

function chargerBon(type) {
  const sel=document.getElementById(type==='client'?'bon-miss-sel':'bon-conv-sel');
  const idx=parseInt(sel.value);
  const contentId=type==='client'?'bon-client-content':'bon-conv-content';
  if(isNaN(idx)){document.getElementById(contentId).style.display='none';return;}
  const m=getData().missions[idx];
  const cl=state.clients.find(c=>c.id===m.clientId);
  const cv=state.convoyeurs.find(c=>c.id===m.convoyeurId);
  if(type==='client'){
    document.getElementById('bc-code').textContent=m.codeMission||'—';
    document.getElementById('bc-fac').textContent=m.numFac||'—';
    document.getElementById('bc-date').textContent=m.date||'—';
    document.getElementById('bc-client').textContent=cl?cl.nom:'—';
    document.getElementById('bc-conv').textContent=cv?cv.nom:'—';
    document.getElementById('bc-ht').textContent=eur(m.ht);
    document.getElementById('bc-ttc').textContent=eur((m.ht||0)*1.2);
    document.getElementById('bc-options').innerHTML='';
    document.getElementById('bc-total-options').textContent='0,00 €';
  } else {
    document.getElementById('bv-code').textContent=m.codeMission||'—';
    document.getElementById('bv-date').textContent=m.date||'—';
    document.getElementById('bv-conv').textContent=cv?cv.nom:'—';
    document.getElementById('bv-remun').textContent=eur(m.conv);
    document.getElementById('bv-options').innerHTML='';
  }
  document.getElementById(contentId).style.display='block';
}

function ajouterOption(type) {
  const cont=document.getElementById(type==='client'?'bc-options':'bv-options');
  const row=document.createElement('div');
  row.style.cssText='display:flex;gap:6px;margin-bottom:8px;align-items:center;';
  row.innerHTML=`<select style="flex:1;padding:8px;border:1px solid var(--border);border-radius:8px;background:var(--jaune);font-size:13px;">
    <option value="">-- Option --</option>
    ${OPTIONS_LIST.map(o=>`<option>${o}</option>`).join('')}
  </select>
  ${type==='client'?`<input type="number" placeholder="Prix HT €" step="0.01" style="width:90px;padding:8px;border:1px solid var(--border);border-radius:8px;background:var(--jaune);font-size:13px;" onchange="majTotalOptions()">`:
  `<input type="text" placeholder="Note…" style="flex:1;padding:8px;border:1px solid var(--border);border-radius:8px;background:var(--jaune);font-size:13px;">`}
  <button onclick="this.parentElement.remove();${type==='client'?'majTotalOptions()':''}" style="background:var(--rouge);color:white;border:none;border-radius:6px;padding:8px;cursor:pointer;font-size:12px;">✕</button>`;
  cont.appendChild(row);
}

function majTotalOptions() {
  const inputs=document.querySelectorAll('#bc-options input[type=number]');
  const tot=Array.from(inputs).reduce((a,i)=>a+(parseFloat(i.value)||0),0);
  document.getElementById('bc-total-options').textContent=eur(tot);
}

function imprimerBon() {
  // Déterminer quel bon est actif
  const isClient=document.getElementById('bon-client-panel').style.display!=='none';
  const bonId=isClient?'bon-client-print':'bon-conv-print';
  const bon=document.getElementById(bonId);
  if(!bon){showToast('⚠️ Aucun bon chargé');return;}

  // Cloner et remplacer les inputs par leur valeur texte
  const clone=bon.cloneNode(true);
  clone.querySelectorAll('input,textarea').forEach(el=>{
    const orig=document.getElementById(el.id);
    const val=orig?orig.value:'';
    const div=document.createElement('div');
    div.style.cssText='padding:5px 0;font-size:14px;font-weight:500;min-height:20px;';
    div.textContent=val||'—';
    el.parentNode.replaceChild(div,el);
  });
  clone.querySelectorAll('select').forEach(el=>{
    const orig=document.getElementById(el.id);
    const val=orig&&orig.selectedIndex>=0?orig.options[orig.selectedIndex].text:'';
    const div=document.createElement('div');
    div.style.cssText='padding:5px 0;font-size:14px;font-weight:500;';
    div.textContent=val||'—';
    el.parentNode.replaceChild(div,el);
  });
  clone.querySelectorAll('button').forEach(b=>b.remove());

  const win=window.open('','_blank');
  win.document.write(`<!DOCTYPE html><html><head><meta charset="UTF-8">
    <title>Bon HC Gestion</title>
    <style>
      *{box-sizing:border-box;margin:0;padding:0;}
      body{font-family:-apple-system,BlinkMacSystemFont,'Helvetica Neue',sans-serif;margin:24px;color:#1a1a2e;background:white;}
      .bon-head{padding:18px;text-align:center;background:#1F3864;color:white;border-radius:8px 8px 0 0;}
      .bon-body{padding:18px;border:1px solid #DDE2EC;border-top:none;border-radius:0 0 8px 8px;}
      .bon-section{margin-bottom:14px;padding-bottom:14px;border-bottom:1px solid #DDE2EC;}
      .bon-section:last-child{border-bottom:none;margin-bottom:0;}
      .bon-label{font-size:10px;font-weight:700;color:#888;text-transform:uppercase;letter-spacing:.3px;margin-bottom:3px;}
      .bon-val{font-size:14px;font-weight:600;}
      .fr3{display:grid;grid-template-columns:1fr 1fr 1fr;gap:12px;margin-bottom:10px;}
      .fr{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-bottom:10px;}
      .cm{font-family:monospace;font-weight:700;color:#1E5631;background:#D9EAD3;padding:2px 6px;border-radius:4px;font-size:13px;}
      .mp{color:#1E5631;font-weight:700;}
      @media print{body{margin:0;}}
    </style>
  </head><body>${clone.outerHTML}</body></html>`);
  win.document.close();
  win.focus();
  setTimeout(()=>{win.print();},500);
  showToast('📄 Bon ouvert → Enregistrer en PDF');
}

// ═══════════════════════════════════════════════════════════════════
// BILAN
// ═══════════════════════════════════════════════════════════════════
function refreshBilan() {
  const d=getData();const s=calcSoldes();
  const totCA=d.missions.reduce((a,m)=>a+(m.ht||0),0);
  const charges=d.compta.filter(e=>e.cat==='Charge').reduce((a,e)=>{
    return a+(e.nd||0)+(e.t55||0)/1.055+(e.t10||0)/1.1+(e.t20||0)/1.2+(e.st||0);
  },0);
  const immob=d.compta.filter(e=>e.cat==='Immobilisation').reduce((a,e)=>{
    return a+(e.nd||0)+(e.t55||0)/1.055+(e.t10||0)/1.1+(e.t20||0)/1.2+(e.st||0);
  },0);
  const benef=d.missions.reduce((a,m)=>a+calcMission(m).benef,0);
  const convDu=d.missions.reduce((a,m)=>a+(m.conv||0),0);
  const convVerse=d.compta.filter(e=>e.type==='Paiement convoyeur').reduce((a,e)=>a+(e.st||0),0);
  const convReste=convDu-convVerse;
  const cliReste=state.clients.reduce((a,cl)=>{
    const du=d.missions.filter(m=>m.clientId===cl.id).reduce((b,m)=>b+(m.ht||0),0);
    const enc=d.compta.filter(e=>['Recette client','Recette especes'].includes(e.type)&&e.cliId===cl.id).reduce((b,e)=>{
      return b+(e.nd||0)+(e.t55||0)/1.055+(e.t10||0)/1.1+(e.t20||0)/1.2+(e.st||0);
    },0);
    return a+(du-enc);
  },0);

  const actifItems=[
    {label:'Immobilisations HT',val:immob,auto:true},
    {label:'Créances clients HT',val:cliReste,auto:true},
    {label:'Solde bancaire',val:s.banque,auto:true},
    {label:'Caisse espèces',val:s.caisse,auto:true},
  ];
  const passifItems=[
    {label:'Charges HT',val:charges,auto:true},
    {label:'Dette CCA',val:s.cca,auto:true},
    {label:'Dettes convoyeurs',val:convReste,auto:true},
    {label:'TVA nette à reverser',val:s.tva,auto:true},
    {label:'Bénéfice brut HT',val:benef,auto:true},
  ];

  const totA=actifItems.reduce((a,i)=>a+i.val,0);
  const totP=passifItems.reduce((a,i)=>a+i.val,0);

  document.getElementById('bilan-actif').innerHTML=actifItems.map(i=>`
    <div class="bilan-row"><span>${i.label}</span><span class="mt mp">${eur(i.val)}</span></div>`).join('');
  document.getElementById('bilan-passif').innerHTML=passifItems.map(i=>`
    <div class="bilan-row"><span>${i.label}</span><span class="mt" style="color:var(--orng);">${eur(i.val)}</span></div>`).join('');
  document.getElementById('bilan-total-actif').textContent=eur(totA);
  document.getElementById('bilan-total-passif').textContent=eur(totP);

  // Créances et dettes manuelles
  document.getElementById('bilan-creances').innerHTML=state.bilanManuel.creances.map((c,i)=>`
    <div style="display:flex;gap:8px;align-items:center;margin-bottom:6px;">
      <input type="text" value="${c.label}" style="flex:1;padding:7px;border:1px solid var(--border);border-radius:6px;font-size:13px;background:var(--jaune);" onchange="state.bilanManuel.creances[${i}].label=this.value;sauverState();">
      <input type="number" value="${c.val}" style="width:110px;padding:7px;border:1px solid var(--border);border-radius:6px;font-size:13px;background:var(--jaune);" step="0.01" onchange="state.bilanManuel.creances[${i}].val=parseFloat(this.value)||0;sauverState();">
      <button onclick="state.bilanManuel.creances.splice(${i},1);sauverState();refreshBilan();" style="background:var(--rouge);color:white;border:none;border-radius:6px;padding:7px;cursor:pointer;">✕</button>
    </div>`).join('');
  document.getElementById('bilan-dettes').innerHTML=state.bilanManuel.dettes.map((d,i)=>`
    <div style="display:flex;gap:8px;align-items:center;margin-bottom:6px;">
      <input type="text" value="${d.label}" style="flex:1;padding:7px;border:1px solid var(--border);border-radius:6px;font-size:13px;background:var(--jaune);" onchange="state.bilanManuel.dettes[${i}].label=this.value;sauverState();">
      <input type="number" value="${d.val}" style="width:110px;padding:7px;border:1px solid var(--border);border-radius:6px;font-size:13px;background:var(--jaune);" step="0.01" onchange="state.bilanManuel.dettes[${i}].val=parseFloat(this.value)||0;sauverState();">
      <button onclick="state.bilanManuel.dettes.splice(${i},1);sauverState();refreshBilan();" style="background:var(--rouge);color:white;border:none;border-radius:6px;padding:7px;cursor:pointer;">✕</button>
    </div>`).join('');
}

function ajouterCreance(){state.bilanManuel.creances.push({label:'Client — facture…',val:0});sauverState();refreshBilan();}
function ajouterDette(){state.bilanManuel.dettes.push({label:'Fournisseur…',val:0});sauverState();refreshBilan();}

// ═══════════════════════════════════════════════════════════════════
// RÉFÉRENCES
// ═══════════════════════════════════════════════════════════════════
function refreshRefs() {
  document.getElementById('tb-clients').innerHTML=state.clients.map((c,i)=>`<tr>
    <td style="font-weight:600;">${c.nom}</td>
    <td><span class="cm">${c.abrev}</span></td>
    <td><span class="badge bb">${c.delai}</span></td>
    <td><button class="btn btn-g btn-sm" onclick="suppClient(${i})">🗑</button></td>
  </tr>`).join('')||'<tr><td colspan="4" style="text-align:center;padding:20px;color:#aaa;">Aucun client</td></tr>';

  document.getElementById('tb-convs').innerHTML=state.convoyeurs.map((c,i)=>{
    const badgeClass=c.statut==='Gérant'?'bv':c.statut==='AE'?'bb':c.statut==='SOC'?'bo':c.statut==='CDI'?'bg':'bg';
    const isInterne=c.statut==='Gérant'||c.statut==='CDI'||c.statut==='CDD';
    return`<tr>
    <td style="font-weight:600;">${c.nom} ${isInterne?'<span style="font-size:9px;color:#888;">(interne)</span>':''}</td>
    <td><span class="badge ${badgeClass}">${c.statut}</span></td>
    <td><span class="badge bo" style="font-size:9px;">${isInterne?'N/A':c.delai}</span></td>
    <td style="font-size:11px;color:#888;">${c.iban||'—'}</td>
    <td><button class="btn btn-g btn-sm" onclick="suppConv(${i})">🗑</button></td>
  </tr>`;}).join('')||'<tr><td colspan="5" style="text-align:center;padding:20px;color:#aaa;">Aucun convoyeur</td></tr>';

  const p=getData().params||{};
  document.getElementById('p-mois').value=state.moisCourant.slice(0,2);
  document.getElementById('p-annee').value=state.moisCourant.slice(2);
  document.getElementById('p-banque-init').value=p.banqueInit||0;
  document.getElementById('p-caisse-init').value=p.caisseInit||0;
  document.getElementById('p-cca-init').value=p.ccaInit||0;
  document.getElementById('p-tva-init').value=p.tvaInit||0;

  updateSelects();
}

function sauverClient(){
  const nom=document.getElementById('cl-nom').value.trim();
  const abrev=document.getElementById('cl-abrev').value.trim().toUpperCase();
  const delai=document.getElementById('cl-delai').value;
  if(!nom||!abrev){showToast('⚠️ Nom et abréviation requis');return;}
  if(state.clients.some(c=>c.abrev===abrev)){showToast('⚠️ Abréviation déjà utilisée');return;}
  state.clients.push({id:Date.now(),nom,abrev,delai});
  sauverState();closeModal('modal-client');refreshRefs();showToast('✅ '+nom+' ajouté');
}

function majDelaiConv(){
  const statut=document.getElementById('cv-stat').value;
  const delaiSel=document.getElementById('cv-delai');
  if(statut==='Gérant'||statut==='CDI'||statut==='CDD'){
    delaiSel.value='na';
  }
}

function sauverConv(){
  const nom=document.getElementById('cv-nom').value.trim();
  const statut=document.getElementById('cv-stat').value;
  const delai=document.getElementById('cv-delai').value;
  const iban=document.getElementById('cv-iban').value.trim();
  if(!nom){showToast('⚠️ Nom requis');return;}
  state.convoyeurs.push({id:Date.now(),nom,statut,delai,iban});
  sauverState();closeModal('modal-conv');refreshRefs();showToast('✅ '+nom+' ajouté');
}

function suppClient(i){if(!confirm('Supprimer ?'))return;state.clients.splice(i,1);sauverState();refreshRefs();showToast('Supprimé');}
function suppConv(i){if(!confirm('Supprimer ?'))return;state.convoyeurs.splice(i,1);sauverState();refreshRefs();showToast('Supprimé');}

function sauverParams(){
  const mois=document.getElementById('p-mois').value.padStart(2,'0');
  const annee=document.getElementById('p-annee').value.padStart(2,'0');
  state.moisCourant=mois+annee;
  if(!state.mois[state.moisCourant]) state.mois[state.moisCourant]={missions:[],compta:[],params:{}};
  state.mois[state.moisCourant].params={
    banqueInit:parseFloat(document.getElementById('p-banque-init').value)||0,
    caisseInit:parseFloat(document.getElementById('p-caisse-init').value)||0,
    ccaInit:parseFloat(document.getElementById('p-cca-init').value)||0,
    tvaInit:parseFloat(document.getElementById('p-tva-init').value)||0,
  };
  sauverState();updateMoisLabel();showToast('✅ Paramètres enregistrés');
}

// ═══════════════════════════════════════════════════════════════════
// MODAL MOIS
// ═══════════════════════════════════════════════════════════════════
function ouvrirModalMois(){
  const el=document.getElementById('liste-mois-disponibles');
  const moisList=Object.keys(state.mois).sort().reverse();
  if(!moisList.length){el.innerHTML='<p style="color:#888;font-size:13px;">Aucun mois créé</p>';}
  else {
    el.innerHTML=moisList.map(mk=>{
      const label=mk.slice(0,2)+'/20'+mk.slice(2);
      const actif=mk===state.moisCourant;
      return`<div onclick="changerMois('${mk}')" style="display:flex;justify-content:space-between;align-items:center;padding:12px;border-radius:8px;margin-bottom:8px;background:${actif?'var(--bleu)':'var(--gris-l)'};color:${actif?'white':'var(--texte)'};cursor:pointer;">
        <span style="font-weight:600;">${label}</span>
        <span style="font-size:12px;">${actif?'✓ Actuel':''}</span>
      </div>`;
    }).join('');
  }
  openModal('modal-mois');
}

function changerMois(mk){
  state.moisCourant=mk;sauverState();closeModal('modal-mois');
  updateMoisLabel();
  // Rafraîchir toutes les pages pour bien isoler les données du mois
  refreshDash();refreshMissions();refreshCompta();refreshConvoyeurs();refreshClients();refreshBons();refreshBilan();
  showToast('Mois: '+mk.slice(0,2)+'/20'+mk.slice(2));
}

function nouveauMois(){
  const mois=prompt('Mois (MM) :','06');
  const annee=prompt('Année (AA) :','26');
  if(!mois||!annee)return;
  const mk=mois.padStart(2,'0')+annee.padStart(2,'0');
  if(!state.mois[mk]) state.mois[mk]={missions:[],compta:[],params:{banqueInit:0,caisseInit:0,ccaInit:0,tvaInit:0}};
  changerMois(mk);
}

// ═══════════════════════════════════════════════════════════════════
// UTILS
// ═══════════════════════════════════════════════════════════════════
function n(id){return parseFloat(document.getElementById(id).value)||0;}

function updateMoisLabel(){
  const mk=state.moisCourant;
  const moisNoms=['Jan','Fév','Mar','Avr','Mai','Jun','Jul','Aoû','Sep','Oct','Nov','Déc'];
  const m=parseInt(mk.slice(0,2))-1;
  const annee='20'+mk.slice(2);
  document.getElementById('mois-label').textContent=moisNoms[m]+' '+annee;
}

function updateSelects(){
  ['m-client','c-cli'].forEach(id=>{
    const el=document.getElementById(id);if(!el)return;
    const v=el.value;
    el.innerHTML='<option value="">-- Choisir --</option>'+
      state.clients.map(c=>`<option value="${c.id}">${c.nom}</option>`).join('');
    el.value=v;
  });
  ['m-convoyeur','c-conv'].forEach(id=>{
    const el=document.getElementById(id);if(!el)return;
    const v=el.value;
    el.innerHTML='<option value="">-- Choisir --</option>'+
      state.convoyeurs.map(c=>`<option value="${c.id}">${c.nom} (${c.statut})</option>`).join('');
    el.value=v;
  });
}

function toggleAccord(header){
  const item=header.parentElement;
  item.classList.toggle('open');
}

function openModal(id){
  updateSelects();
  if(id==='modal-recurrentes') {
    ouvrirRecurrentes();
    // Afficher rappels du jour
    const rappels=getRappelsRecurrents();
    if(rappels.length) {
      setTimeout(()=>{
        const el=document.getElementById('liste-recurrentes');
        const rappelHtml=`<div style="background:#FFF3CD;border:1px solid #FFEAA7;border-radius:8px;padding:12px;margin-bottom:12px;">
          <div style="font-weight:700;color:#856404;margin-bottom:8px;">⚠️ ${rappels.length} rappel(s) à confirmer ce mois</div>
          ${rappels.map((rec,i)=>`<div style="display:flex;justify-content:space-between;align-items:center;padding:6px 0;border-bottom:1px solid #FFEAA7;">
            <span style="font-size:13px;">${rec.desc} — <strong>${eur(rec.montant)}</strong></span>
            <button class="btn btn-sm" style="background:#856404;color:white;" onclick="confirmerRecurrente(${state.recurrentes.indexOf(rec)})">✅ Confirmer</button>
          </div>`).join('')}
        </div>`+el.innerHTML;
        el.innerHTML=rappelHtml;
      },50);
    }
    document.getElementById(id).classList.add('open');
    return;
  }
  const today=new Date().toISOString().split('T')[0];
  ['m-date','c-date'].forEach(f=>{const el=document.getElementById(f);if(el&&!el.value)el.value=today;});
  document.getElementById(id).classList.add('open');
}
function closeModal(id){document.getElementById(id).classList.remove('open');}
document.querySelectorAll('.modal-overlay').forEach(o=>o.addEventListener('click',e=>{if(e.target===o)o.classList.remove('open');}));

function showToast(msg){
  const t=document.getElementById('toast');t.textContent=msg;t.classList.add('show');
  setTimeout(()=>t.classList.remove('show'),2500);
}

function syncDrive(){
  const blob=new Blob([JSON.stringify(state,null,2)],{type:'application/json'});
  const url=URL.createObjectURL(blob);const a=document.createElement('a');
  a.href=url;a.download='hcgestion_'+state.moisCourant+'_'+new Date().toISOString().slice(0,10)+'.json';
  document.body.appendChild(a);a.click();document.body.removeChild(a);URL.revokeObjectURL(url);
  showToast('☁️ Fichier téléchargé → déplacer dans Drive');
}

function exporterJSON(){syncDrive();}

function importerJSON(input){
  const file=input.files[0];if(!file)return;
  const reader=new FileReader();
  reader.onload=e=>{
    try{
      const imported=JSON.parse(e.target.result);
      if(imported.clients&&imported.mois){
        state=imported;sauverState();refreshDash();refreshRefs();
        showToast('✅ Données importées');
      } else showToast('⚠️ Format invalide');
    }catch(err){showToast('⚠️ Erreur import');}
  };
  reader.readAsText(file);
}

// ═══════════════════════════════════════════════════════════════════
// MODIFIER MISSION & COMPTA
// ═══════════════════════════════════════════════════════════════════
function modifierMission(i) {
  const m=getData().missions[i];
  if(!m) return;
  // Pré-remplir le formulaire
  openModal('modal-mission');
  setTimeout(()=>{
    document.getElementById('m-date').value=m.date||'';
    document.getElementById('m-client').value=m.clientId||'';
    majFacturesClient();
    document.getElementById('m-facture').value=m.numFac||'__new__';
    document.getElementById('m-convoyeur').value=m.convoyeurId||'';
    document.getElementById('m-ht').value=m.ht||'';
    document.getElementById('m-conv').value=m.conv||'';
    document.getElementById('m-carbu').value=m.carbu||'';
    document.getElementById('m-peage').value=m.peage||'';
    document.getElementById('m-lavage').value=m.lavage||'';
    document.getElementById('m-ff').value=m.ff||'';
    document.getElementById('m-sm').value=m.sm||'';
    document.getElementById('m-ta').value=m.ta||'';
    document.getElementById('m-tr').value=m.tr||'';
    document.getElementById('m-hst').value=m.hst||'';
    document.getElementById('m-autre').value=m.autre||'';
    document.getElementById('m-lien').value=m.lien||'';
    // Changer le bouton pour "Mettre à jour"
    const btn=document.querySelector('#modal-mission .btn-v');
    if(btn){btn.textContent='💾 Mettre à jour';btn.onclick=()=>mettreAJourMission(i);}
  },100);
}

function mettreAJourMission(i) {
  const m=getData().missions[i];
  if(!m) return;
  const dateVal=document.getElementById('m-date').value;
  const convId=parseInt(document.getElementById('m-convoyeur').value)||null;
  const convObj=state.convoyeurs.find(c=>c.id===convId);
  const isInterne=convObj&&['Gérant','CDI','CDD'].includes(convObj.statut);
  m.date=dateVal;
  m.clientId=parseInt(document.getElementById('m-client').value)||m.clientId;
  m.convoyeurId=convId;
  m.numFac=document.getElementById('m-facture').value===('__new__')?m.numFac:document.getElementById('m-facture').value;
  m.ht=n('m-ht');m.conv=isInterne?0:n('m-conv');
  m.carbu=n('m-carbu');m.peage=n('m-peage');m.lavage=n('m-lavage');
  m.ff=n('m-ff');m.sm=n('m-sm');m.ta=n('m-ta');m.tr=n('m-tr');
  m.hst=n('m-hst');m.autre=n('m-autre');m.lien=document.getElementById('m-lien').value;
  sauverState();closeModal('modal-mission');
  // Remettre le bouton original
  const btn=document.querySelector('#modal-mission .btn-v');
  if(btn){btn.textContent='Enregistrer';btn.onclick=sauverMission;}
  refreshMissions();refreshDash();showToast('✅ Mission mise à jour');
}

function modifierCompta(i) {
  const e=getData().compta[i];
  if(!e) return;
  openModal('modal-compta');
  setTimeout(()=>{
    document.getElementById('c-date').value=e.date||'';
    document.getElementById('c-type').value=e.type||'';
    document.getElementById('c-cat').value=e.cat||'';
    document.getElementById('c-scat').value=e.scat||'';
    document.getElementById('c-conv').value=e.convId||'';
    document.getElementById('c-cli').value=e.cliId||'';
    document.getElementById('c-ref').value=e.ref||'';
    document.getElementById('c-desc').value=e.desc||'';
    document.getElementById('c-nd').value=e.nd||'';
    document.getElementById('c-55').value=e.t55||'';
    document.getElementById('c-10').value=e.t10||'';
    document.getElementById('c-20').value=e.t20||'';
    document.getElementById('c-st').value=e.st||'';
    document.getElementById('c-lien').value=e.lien||'';
    const btn=document.querySelector('#modal-compta .btn-p');
    if(btn){btn.textContent='💾 Mettre à jour';btn.onclick=()=>mettreAJourCompta(i);}
  },100);
}

function mettreAJourCompta(i) {
  const e=getData().compta[i];
  if(!e) return;
  e.date=document.getElementById('c-date').value;
  e.type=document.getElementById('c-type').value;
  e.cat=document.getElementById('c-cat').value;
  e.scat=document.getElementById('c-scat').value;
  e.convId=parseInt(document.getElementById('c-conv').value)||null;
  e.cliId=parseInt(document.getElementById('c-cli').value)||null;
  e.ref=document.getElementById('c-ref').value;
  e.desc=document.getElementById('c-desc').value;
  e.nd=n('c-nd');e.t55=n('c-55');e.t10=n('c-10');e.t20=n('c-20');e.st=n('c-st');
  e.lien=document.getElementById('c-lien').value;
  sauverState();closeModal('modal-compta');
  const btn=document.querySelector('#modal-compta .btn-p');
  if(btn){btn.textContent='Enregistrer';btn.onclick=sauverCompta;}
  refreshCompta();refreshDash();showToast('✅ Écriture mise à jour');
}
// ═══════════════════════════════════════════════════════════════════
function ajouterJustif(i) {
  const d=getData();
  const e=d.compta[i];
  if(!e) return;
  const expl=prompt('Explication (ex: ticket perdu, paiement espèces sans reçu…) :','');
  if(expl===null) return;
  if(expl.trim()) {
    e.justifExpl=expl.trim();
    sauverState();refreshCompta();showToast('✅ Explication enregistrée');
  } else {
    // Proposer un lien Drive
    const lien=prompt('Ou colle un lien justificatif (Drive, photo…) :','');
    if(lien&&lien.trim()) {
      e.lien=lien.trim();
      sauverState();refreshCompta();showToast('✅ Justificatif ajouté');
    }
  }
}

// ═══════════════════════════════════════════════════════════════════
// ÉCRITURES RÉCURRENTES
// ═══════════════════════════════════════════════════════════════════
function ouvrirRecurrentes() {
  const r=state.recurrentes||[];
  const el=document.getElementById('liste-recurrentes');
  if(!el) return;
  if(!r.length){el.innerHTML='<div class="empty"><div class="ei">🔄</div><div class="et">Aucune écriture récurrente</div></div>';return;}
  el.innerHTML=r.map((rec,i)=>`
    <div style="display:flex;justify-content:space-between;align-items:center;padding:10px 14px;border-bottom:1px solid var(--border);">
      <div>
        <div style="font-weight:600;">${rec.desc} <span class="badge bo">${rec.type}</span></div>
        <div style="font-size:11px;color:#888;">Le ${rec.jour} de chaque mois · ${eur(rec.montant)}</div>
      </div>
      <button class="btn btn-r btn-sm" onclick="suppRecurrente(${i})">🗑</button>
    </div>`).join('');
}

function sauverRecurrente() {
  if(!state.recurrentes) state.recurrentes=[];
  const desc=document.getElementById('rec-desc').value.trim();
  const type=document.getElementById('rec-type').value;
  const jour=parseInt(document.getElementById('rec-jour').value)||1;
  const montant=parseFloat(document.getElementById('rec-montant').value)||0;
  const col=document.getElementById('rec-col').value;
  if(!desc||!montant){showToast('⚠️ Description et montant requis');return;}
  state.recurrentes.push({id:Date.now(),desc,type,jour,montant,col});
  sauverState();closeModal('modal-recurrente');ouvrirRecurrentes();
  showToast('✅ Récurrence ajoutée');
}

function suppRecurrente(i){
  if(!confirm('Supprimer ?'))return;
  state.recurrentes.splice(i,1);sauverState();ouvrirRecurrentes();showToast('Supprimée');
}

function confirmerRecurrente(i) {
  // Crée l'écriture comptable réelle pour cette récurrence aujourd'hui
  const rec=state.recurrentes[i];
  if(!rec) return;
  const today=new Date().toISOString().split('T')[0];
  const e={
    id:Date.now(),date:today,type:rec.type,cat:'Charge',scat:rec.desc,
    convId:null,cliId:null,ref:'REC-'+rec.id,desc:rec.desc,
    nd:0,t55:0,t10:0,t20:0,st:0,lien:'',justifExpl:'Récurrence automatique confirmée',
  };
  e[rec.col||'t20']=rec.montant;
  getData().compta.push(e);
  sauverState();refreshCompta();refreshDash();
  showToast('✅ '+rec.desc+' enregistré en comptabilité');
}

function getRappelsRecurrents() {
  // Retourne les récurrences dont le jour est aujourd'hui ou passé ce mois et non encore confirmées
  if(!state.recurrentes||!state.recurrentes.length) return [];
  const today=new Date();
  const jourAuj=today.getDate();
  const d=getData();
  return state.recurrentes.filter(rec=>{
    if(rec.jour>jourAuj) return false; // pas encore ce mois
    const dejaSaisi=d.compta.some(e=>e.ref==='REC-'+rec.id&&e.date&&e.date.slice(0,7)===today.toISOString().slice(0,7));
    return !dejaSaisi;
  });
}

// ═══════════════════════════════════════════════════════════════════
// ÉCHÉANCES CONVOYEURS
// ═══════════════════════════════════════════════════════════════════
function calcEcheanceConv(dateMission, delai) {
  const d=new Date(dateMission);
  const jour=d.getDate();
  const mois=d.getMonth();
  const annee=d.getFullYear();
  let ech;
  if(delai==='quinzaine') {
    if(jour<=15) { ech=new Date(annee,mois+1,1); }
    else { ech=new Date(annee,mois+1,16); }
  } else if(delai==='mois') {
    ech=new Date(annee,mois+1,1);
  } else if(delai==='30jfm') {
    const finMois=new Date(annee,mois+1,0);
    ech=new Date(finMois.getTime()+30*86400000);
  } else if(delai==='45jfm') {
    const finMois=new Date(annee,mois+1,0);
    ech=new Date(finMois.getTime()+45*86400000);
  } else {
    ech=new Date(d.getTime()+2*86400000); // immédiat = 2 jours
  }
  return ech;
}

function statutEcheance(dateEch) {
  // Rouge : lundi de la semaine suivant le dépassement
  // Orange : même semaine que l'échéance ou dans les 10 jours avant
  const today=new Date(); today.setHours(0,0,0,0);
  const ech=new Date(dateEch); ech.setHours(0,0,0,0);
  const diff=Math.floor((ech-today)/86400000); // jours restants

  if(diff>10) return {statut:'ok',label:'',color:''};
  if(diff>=0) return {statut:'orange',label:'À payer bientôt',color:'#FF8C00'};

  // Dépassé — est-on encore dans la même semaine de travail ?
  // Trouver le lundi suivant l'échéance
  const jourSem=ech.getDay(); // 0=dim, 1=lun...
  const joursJusquLundi=jourSem===0?1:(8-jourSem)%7||7;
  const lundiSuivant=new Date(ech.getTime()+joursJusquLundi*86400000);
  lundiSuivant.setHours(0,0,0,0);

  if(today<lundiSuivant) return {statut:'orange',label:'À payer cette semaine',color:'#FF8C00'};
  return {statut:'rouge',label:'En retard',color:'#E74C3C'};
}

// ═══════════════════════════════════════════════════════════════════
// PWA — SERVICE WORKER & ICÔNE
// ═══════════════════════════════════════════════════════════════════
function initPWA() {
  // Générer l'icône dynamiquement via canvas
  const canvas = document.createElement('canvas');
  canvas.width = 192; canvas.height = 192;
  const ctx = canvas.getContext('2d');
  // Fond bleu
  ctx.fillStyle = '#1F3864';
  ctx.beginPath();
  ctx.roundRect(0, 0, 192, 192, 32);
  ctx.fill();
  // Emoji voiture
  ctx.font = '90px serif';
  ctx.textAlign = 'center';
  ctx.textBaseline = 'middle';
  ctx.fillText('🚗', 96, 86);
  // Texte HC
  ctx.fillStyle = 'white';
  ctx.font = 'bold 28px -apple-system, sans-serif';
  ctx.fillText('HC', 96, 155);
  const iconUrl = canvas.toDataURL('image/png');

  // Apple touch icon
  const link = document.createElement('link');
  link.rel = 'apple-touch-icon';
  link.href = iconUrl;
  document.head.appendChild(link);

  // Manifest dynamique
  const manifest = {
    name: 'HC Gestion',
    short_name: 'HC Gestion',
    description: 'Gestion comptable Hexagone Confluence',
    start_url: './',
    display: 'standalone',
    background_color: '#1F3864',
    theme_color: '#1F3864',
    orientation: 'portrait-primary',
    icons: [
      { src: iconUrl, sizes: '192x192', type: 'image/png' },
      { src: iconUrl, sizes: '512x512', type: 'image/png' }
    ]
  };
  const blob = new Blob([JSON.stringify(manifest)], {type:'application/json'});
  const mUrl = URL.createObjectURL(blob);
  const mLink = document.querySelector('link[rel="manifest"]');
  if(mLink) mLink.href = mUrl;

  // Service Worker pour fonctionnement hors ligne
  if('serviceWorker' in navigator) {
    const swCode = `
      const CACHE = 'hcgestion-v2';
      self.addEventListener('install', e => {
        e.waitUntil(caches.open(CACHE).then(c => c.addAll(['/'])));
        self.skipWaiting();
      });
      self.addEventListener('activate', e => {
        e.waitUntil(caches.keys().then(keys =>
          Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))
        ));
      });
      self.addEventListener('fetch', e => {
        e.respondWith(caches.match(e.request).then(r => r || fetch(e.request).catch(() => caches.match('/'))));
      });
    `;
    const swBlob = new Blob([swCode], {type:'application/javascript'});
    const swUrl = URL.createObjectURL(swBlob);
    navigator.serviceWorker.register(swUrl).catch(()=>{});
  }

  // Bannière "Ajouter à l'écran d'accueil" si pas encore installée
  if(!window.matchMedia('(display-mode: standalone)').matches && !navigator.standalone) {
    setTimeout(()=>{
      const banner = document.createElement('div');
      banner.id = 'pwa-banner';
      banner.innerHTML = `<div style="position:fixed;bottom:0;left:0;right:0;background:#1F3864;color:white;padding:14px 16px;z-index:500;display:flex;align-items:center;justify-content:space-between;box-shadow:0 -2px 10px rgba(0,0,0,.3);">
        <div><div style="font-weight:700;font-size:13px;">📲 Installer HC Gestion</div><div style="font-size:11px;opacity:.8;">Safari → Partager → Ajouter à l'écran d'accueil</div></div>
        <button onclick="document.getElementById('pwa-banner').remove()" style="background:rgba(255,255,255,.2);border:none;color:white;padding:6px 12px;border-radius:14px;font-size:12px;cursor:pointer;">✕ Fermer</button>
      </div>`;
      document.body.appendChild(banner);
    }, 3000);
  }
}

// ═══════════════════════════════════════════════════════════════════
// INIT
// ═══════════════════════════════════════════════════════════════════
chargerState();
updateMoisLabel();
refreshDash();
refreshRefs();
initPWA();
</script>
</body>
</html>
